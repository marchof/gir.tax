# oecd-gir-viewer

This project keeps its living specifications in [specs/](specs/). Before working
on a concern that one of them covers, read [specs/README.md](specs/README.md) and
the spec it indexes for that area. Treat the relevant spec as the source of truth
for its conventions, and update it in place if a deliberate change makes it
outdated.
