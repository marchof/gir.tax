xmlnamespaces:
  globe: "urn:oecd:ties:globe:v2"
  iso: "urn:oecd:ties:isoglobetypes:v1"
  stf: "urn:oecd:ties:globestf:v5"

rules:

  - number: 60001
    targets:
      - /globe:GLOBE_OECD/globe:MessageSpec/globe:MessageRefId
    element: MessageRefld
    rule: >-
      The MessageRefld must be in the correct format: [CountryCode of the sending
      jurisdiction] [Reporting Period] [CountryCode of the receiving jurisdiction]
      [UniqueID]
    description: >-
      The structure of the MessageRefld is not in the correct format. Please ensure
      the MessageRefld follows structure defined in the GIR User Guide, and resubmit
      the file.

  - number: 60002
    targets:
      - /globe:GLOBE_OECD/globe:MessageSpec/globe:MessageRefId
    element: MessageRefld
    rule: >-
      The GIR Message Header has a duplicate MessageRefld value that was received on
      a previous file.
    description: >-
      Please replace the MessageRefld field value with a new unique value (not
      containing all blanks), and resubmit the file.

  - number: 60003
    targets:
      - /globe:GLOBE_OECD/globe:MessageSpec/globe:ReportingPeriod
    element: ReportingPeriod
    rule: >-
      The ReportingPeriod YYYY value must be less than or equal to current year
    description: >-
      ReportingPeriod year must be less than or equal to current year.

  - number: 60004
    targets:
      - /globe:GLOBE_OECD
    when: //stf:DocTypeIndic = 'OECD1'
    absent: //stf:DocTypeIndic[. = 'OECD2' or . = 'OECD3']
    rule: >-
      A message can contain either new records (OECD1) or corrections (OECD2 or
      OECD3), but should not contain a mixture of both.
    description: >-
      This message contains a mix of new records (OECD1) and corrections
      (OECD2/OECD3). Please ensure the DocTypeIndic elements contain either new
      records (OECD1) or corrections (OECD2 or OECD3), but not a mixture of both.
    implementation_notes: >-
      The rule is a whole-message property, not a per-element one, so the test is
      a document-wide condition, therefore the target has been adjusted to the root
      element of the message.

  - number: 60005
    targets:
      - //stf:DocTypeIndic
    element: DocTypeIndic
    rule: >-
      When DocTypeIndic is OECD2 or OECD3, the record must concern the same sub
      section (FilingInfo, General section, Summary, JuridictionSection or
      UTPRAttribution) as the CorrDocRefld.
    description: >-
      The correction or deletion refers to a record of an other sub section.

  - number: 60006
    targets:
      - /globe:GLOBE_OECD
    distinct: //stf:CorrDocRefId
    rule: >-
      The same DocReflD cannot be corrected or deleted twice in the same message.
    description: >-
      The same DocRefID cannot be corrected or deleted twice in the same message,
      therefore each CorrDocRefID must be unique within the same message.
    implementation_notes: >-
      Uniqueness is scoped to the whole message, so the target is the message
      root and `distinct` collects every CorrDocRefId in it.

  - number: 60007
    targets:
      - //stf:DocRefId
    element: DocRefId
    rule: >-
      The DocRefID is already used for another record.
    description: >-
      The provided DocRefID has already been used for a pre-existing record, please
      relodge and ensure that the provided DocRefID is in accordance with the OECD GIR
      XML Schema User Guide and is 'unique in time and space'.

  - number: 60008
    targets:
      - //stf:CorrDocRefId
    element: CorrDocRefld
    rule: >-
      The CorrDocRefid refers to an unknown record.
    description: >-
      The CorrDocRefld provided does not match to an existing record of a DocRefld,
      please relodge the file and ensure that the CorrDocRefld is an exact match to
      the DocRefld of the element which is subject to a correction

  - number: 60009
    targets:
      - //stf:CorrDocRefId
    element: CorrDocRefld
    rule: >-
      The CorrDocRefID must relate to the latest instance of the DocRefID. The
      CorrDocRefID cannot reference an invalidated or outdated version of the DocRefID
      (i.e. CorrDocRefID refers to the DocRefID of the original record, while a
      subsequent correction has been received which has a new DocRefID/CorrDocRefID)
    description: >-
      The corrected record is no longer valid (invalidated or outdated by a previous
      correction message). As a consequence, no further information should have been
      received on this version of the record.

  - number: 60010
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:FilingInfo/globe:DocSpec/stf:DocTypeIndic
    when: . = 'OECD3'
    absent: >-
      ancestor::globe:GLOBEBody/*[self::globe:GeneralSection or self::globe:Summary
      or self::globe:JurisdictionSection or self::globe:UTPRAttribution]/globe:DocSpec/stf:DocTypeIndic[. != 'OECD3']
    rule: >-
      The FilingInfo cannot be deleted without deleting all related General Section,
      Summary, JurisdictionSection and UTPRAttribution records
    description: >-
      To delete the FilingInfo please ensure that a deletion request has been sent for
      all associated General Section, Summary, JurisdictionSection and UTPRAttribution
      records.

  - number: 60011
    targets:
      - //stf:DocRefId
    element: DocRefld
    rule: >-
      The DocRefld must be in the correct format: [CountryCode of the sending
      jurisdiction] [ReportingYear][UniqueID]
    description: >-
      The structure of the DocRefld is not in the correct format.

  - number: 60012
    targets:
      - //globe:DocSpec
    when: stf:DocTypeIndic = 'OECD0' or stf:DocTypeIndic = 'OECD1'
    absent: stf:CorrDocRefId
    rule: >-
      When DocTypeIndic is OECD1 or OECD0, the CorrDocRefld field must be omitted.
    description: >-
      When an element has DocTypeIndic of OECD1 or OECD0, which indicates that a new
      lodgment or a resend is being made, the CorrDocRefld field must not be
      completed.
    implementation_notes: >-
      Targets the DocSpec (the CorrDocRefId's parent) rather than the CorrDocRefId
      itself, so the consequence "CorrDocRefId must be omitted" can be expressed as
      an `absent` check (a target on the CorrDocRefId could never observe its own
      absence).

  - number: 60013
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:DocSpec/stf:DocTypeIndic
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:Summary/globe:DocSpec/stf:DocTypeIndic
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:DocSpec/stf:DocTypeIndic
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:UTPRAttribution/globe:DocSpec/stf:DocTypeIndic
    notEquals:
      expected: "'OECD0'"
    rule: >-
      The Resend option may only be used with respect to the FilingInfo element. The
      value of OECD0 is not an acceptable input in the DocTypeIndic for the
      GeneralSection, Summary, JurisdictionSection, UTPRAttribution
    description: >-
      OECD0 is only a valid input for the DocTypeIndic of the FilingInfo and should
      not be used for any other element.
    target_does_not_exist_in_test_files:
      - ok-05.xml

  - number: 60014
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:FilingInfo/globe:DocSpec/stf:DocRefId
    element: DocRefld
    rule: >-
      When the DocTypeIndic is OECD0 for the FilingInfo, The DocRefID must be the same
      as the DocReflD that was used for the latest version of the FilingInfo
    description: >-
      An unknown or invalid DocRefID was specified for the Resend option (OECD0).

  - number: 60015
    targets:
      - //globe:DocSpec
    when: stf:DocTypeIndic = 'OECD2' or stf:DocTypeIndic = 'OECD3'
    present: stf:CorrDocRefId
    rule: >-
      The corrected element does not specify any CorrDocRefld (OECD2/3 without
      CorrDocRefld data).
    description: >-
      When DocTypeIndic is "OECD2" or "OECD3", CorrDocRefld must be provided.

  - number: 60016
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:DocSpec/stf:DocTypeIndic
    when: >-
      ../../../globe:FilingInfo/globe:DocSpec/stf:DocTypeIndic = 'OECD0'
    notEquals:
      expected: "'OECD1'"
    rule: >-
      If the value of FilingInfo DoctypeIndic element is OECD0, if General Section is
      provided, the value of General Section DocTypeIndic element must not be OECD1.
    description: >-
      A new General Section can't be filed if one already exists. Please process with
      a correction or deletion if the General Section has to be modified.
    target_does_not_exist_in_test_files:
      - ok-01.xml

  - number: 60017
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody
    when: >-
      globe:FilingInfo/globe:DocSpec/stf:DocTypeIndic = 'OECD1'
    present: globe:GeneralSection
    rule: >-
      If the value of FilingInfo DoctypeIndic element is OECD1, the General Section
      must be provided.
    description: >-
      Each new GIR filing must contain a General Section.

  - number: 60018
    targets:
      - /globe:GLOBE_OECD
    when: //globe:RecJurCode
    equals:
      actual: //globe:RecJurCode
      expected: /globe:GLOBE_OECD/globe:MessageSpec/globe:ReceivingCountry
    rule: >-
      At least one Country ISO Code in RecJurCode must be the same as the
      ReceivingCountry.
    description: >-
      At least one Country ISO Code in RecJurCode must be the same as the
      ReceivingCountry.
    implementation_notes: >-
      Whole-message existence check (one of the many RecJurCode must match the single
      ReceivingCountry), so the target is the message root rather than each RecJurCode
      and `equals` is satisfied when any RecJurCode value equals ReceivingCountry.
      Guarded on the presence of RecJurCode because every RecJurCode-bearing section
      is optional in the schema, so a FilingInfo-only message (e.g. a resend or
      deletion) is valid and carries no RecJurCode for this routing rule to apply to.

  - number: 60019
    targets:
      - //globe:RecJurCode
    when: >-
      ancestor::globe:GLOBEBody/globe:FilingInfo/globe:FilingCE/globe:Role[. = 'GIR403' or . = 'GIR404' or . = 'GIR405']
    equals:
      expected: ancestor::globe:GLOBEBody/globe:FilingInfo/globe:FilingCE/globe:ResCountryCode
    rule: >-
      Where the Role of the Filing CE is GIR403, GIR404, GIR405, then the GIR is only
      a local lodgement and must not be exchanged.
    description: >-
      The RecJurCode should be (name of the local jurisdiction) as the FilingCE role
      provide suggests that the GIR is only a local lodgment and must not be
      exchanged.
    implementation_notes: >-
      "Local jurisdiction" is read as the FilingCE ResCountryCode (the filer's
      residence, tied to the Transmitting Country by 60023). When the same GLOBEBody's
      FilingCE Role is a local-lodgement role (GIR403/404/405), every RecJurCode in
      that GLOBEBody must equal that ResCountryCode; the FilingCE is reached via
      ancestor::globe:GLOBEBody so each filing is checked against its own role/residence.

  - number: 60020
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:FilingInfo/globe:Period/globe:Start
    atMost:
      expected: ../globe:End
      type: date
    rule: >-
      Period start date must not be later than the Period end date
    description: >-
      Period start date must be earlier than or same as Period end date.

  - number: 60021
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:FilingInfo/globe:Period/globe:End
    atMost:
      expected: /globe:GLOBE_OECD/globe:MessageSpec/globe:ReportingPeriod
      type: date
    rule: >-
      Reporting Period End Date must not be later than the Reporting Period in the
      Message Header.
    description: >-
      The Reporting Period End Date must not be a date which is beyond the Reporting
      Period date in the MessageHeader

  - number: 60022
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:FilingInfo/globe:FilingCE/globe:TIN
    when: ../globe:Role = 'GIR401'
    in:
      - ancestor::globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:UPE/globe:ExcludedUPE/globe:ID/globe:TIN
      - ancestor::globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:UPE/globe:OtherUPE/globe:ID/globe:TIN
    rule: >-
      When the role is GIR401, the FilingCE TIN should match at least one of the TINs
      provided in the ID of the UPE element, whether it is OtherUPE or ExcludedUPE
    description: >-
      When the UPE is lodging the GIR, then the contents of the FilingCE must match at
      least one of the TINs provided in the UPE element (either the OtherUPE or
      ExcludedUPE)
    implementation_notes: >-
      Matched by TIN value: `in` holds when the FilingCE TIN equals any ExcludedUPE or
      OtherUPE ID/TIN. The UPE TINs are reached via ancestor::globe:GLOBEBody so the
      FilingCE is matched against its own filing's corporate structure.

  - number: 60023
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:FilingInfo/globe:FilingCE/globe:ResCountryCode
    equals:
      expected: /globe:GLOBE_OECD/globe:MessageSpec/globe:TransmittingCountry
    rule: >-
      The ResCountryCode of the FilingCE must match to the Transmitting Country
    description: >-
      The ResCountryCode of the FilingCE must match to the Transmitting Country

  - number: 60024
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:Summary
    when: >-
      globe:SafeHarbour or globe:ETRRange or globe:SBIE or globe:QDMTTut or globe:GLoBETut
    present: globe:JurWithTaxingRights/globe:JurisdictionName
    rule: >-
      If the Summary contains any of the following elements: SafeHarbour, ETRRange,
      SBIE, QDMTTut, GLoBETut, then the JurWithTaxingRights/JurisdictionName must be
      provided.
    description: >-
      If the Summary contains any of the following elements: SafeHarbour, ETRRange,
      SBIE, QDMTTut, GLoBETut, then the JurWithTaxingRights/JurisdictionName must be
      provided.
    implementation_notes: >-
      Targets the Summary element, not Summary/JurWithTaxingRights, so the violation
      where JurWithTaxingRights/JurisdictionName is entirely absent is still detected
      (a target on the absent element would simply not match and pass vacuously).

  - number: 60025
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:ETRRate
    when: ../globe:NetGlobeIncome/globe:Total > 0
    almostEquals:
      expected: sum(../globe:AdjustedCoveredTax/globe:Total) div ../globe:NetGlobeIncome/globe:Total
    implementation_notes: >-
      Guarded on a positive NetGlobeIncome, the case the rule says it applies to
      (this also avoids the divide-by-zero); a missing AdjustedCoveredTax/Total is
      summed to 0 per the rule.
    rule: >-
      The ETRRate must be equal to the integer value reported at the AdjustedCovered
      Tax/Total element DIVIDED by the integer reported at the NetGlobeIncome/Total
      element. This rule does not apply if the NetGlobeIncome in the jurisdiction is
      zero (or negative). If AdjustedCovered Tax/Total element is not provided, then
      value assumed to be 0.
    description: >-
      The ETRRate must equal AdjustedCovered Tax/Total (Divided by)
      NetGlobeIncome/Total

  - number: 60026
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:TopUpTax
    almostEquals:
      expected: >-
        sum(../globe:TopUpTaxPercentage) * sum(../globe:ExcessProfits) +
        sum(../globe:AdditionalTopUpTax/globe:NONArt4.1.5/globe:AdditionalTopUpTax) +
        sum(../globe:AdditionalTopUpTax/globe:Art4.1.5/globe:AdditionalTopUpTax) -
        sum(../globe:QDMTT/globe:Amount)
    implementation_notes: >-
      Every operand is summed so a missing element counts as 0 per the rule.
    rule: >-
      TopUpTax must equal the following calculation: (TopUp TaxPercentage * Excess
      Profits) + (NONArt.4.1.5/Additional TopUpTax + Art4.1.5AdditionalTopUpTax/) -
      QDMTT/Amount. If any elements are not provided, these should be treated as a 0
      value.
    description: >-
      Note that Additional TopUpTax is an optional field and as such, if no Additional
      TopUpTax element is provided, the calculation should then be read as:
      (TopUpTaxPercentage * Excess Profits) + 0 - QDMTT/Amount

  - number: 60027
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:LowTaxJurisdiction/globe:LTCE/globe:IIR/globe:ParentEntity/globe:TopUpTax
    almostEquals:
      expected: ../globe:TopUpTaxShare - sum(../globe:IIROffSet)
    rule: >-
      The IIR/ParentEntity/TopUp Tax integer must equal the following calculation:
      IIR/ParentEntity/TopUpTaxShare - IIROffSet
    description: >-
      The IIR/ParentEntity/TopUpTax integer must equal the following calculation:
      TopUpTaxShare - IIROffSet

  - number: 60028
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:CEComputation/globe:AdjustedFANIL/globe:Total
    almostEquals:
      expected: >-
        sum(../globe:FANIL) +
        sum(../globe:Adjustment/globe:MainEntityPEandFTE/globe:Additions) +
        sum(../globe:Adjustment/globe:MainEntityPEandFTE/globe:Reductions)
    rule: >-
      The Total must equal the following calculation: AdjustedFANIL/FANIL + (sum of
      all MainEntityPEandFTE/Additions elements) - (sum of all
      MainEntityPEandFTE/Reductions elements)
    description: >-
      The Total must equal the following calculation: FANIL/FANIL + (sum of all Main
      EntityPEandFTE/Additions elements) - (sum of all MainEntityPEandFTE/Reductions
      elements) Adjusted
    implementation_notes: >-
      As the Reductions elements are supposed to be negative we assume that the SUM
      of all Additions and Reduction elements has to be calculated.


  - number: 70001
    targets:
      - //globe:TIN
    when: "@TypeOfTIN = 'GIR3004'"
    allOf:
      - equals:
          expected: "'NOTIN'"
      - isTrue: "@unknown"
      - absent: "@issuedBy"
    rule: >-
      If TypeOfTIN is GIR3004 then TIN must contain the text 'NOTIN', flag Unknown
      must be TRUE and IssuedBy must not be provided
    description: >-
      If TypeOfTIN is GIR3004 then TIN must contain the text 'NOTIN', flag Unknown
      must be TRUE and IssuedBy must not be provided

  - number: 70002
    targets:
      - //globe:TIN
    when: ". = 'NOTIN'"
    allOf:
      - equals:
          actual: "@TypeOfTIN"
          expected: "'GIR3004'"
      - isTrue: "@unknown"
      - absent: "@issuedBy"
    rule: >-
      If TIN value is 'NOTIN' then TypeOfTIN must be GIR3004, flag Unknown must be
      TRUE and IssuedBy must not be provided.
    description: >-
      If TIN value is 'NOTIN' then TypeOfTIN must be GIR3004, flag Unknown must be
      TRUE and IssuedBy must not be provided.

  - number: 70003
    targets:
      - //globe:TIN
    when:
      isTrue: "@unknown"
    allOf:
      - equals:
          expected: "'NOTIN'"
      - equals:
          actual: "@TypeOfTIN"
          expected: "'GIR3004'"
      - absent: "@issuedBy"
    rule: >-
      If flag Unknown is TRUE then TIN must be NOTIN, TypeOfTIN must be GIR3004 and
      IssuedBy must not be provided.
    description: >-
      If flag Unknown is TRUE then TIN must be NOTIN, TypeOfTIN must be GIR3004 and
      IssuedBy must not be provided.

  - number: 70004
    targets:
      - //globe:TIN
    element: issuedBy
    rule: >-
      Where the TIN has the issuedby value of (local jurisdiction), then the TIN
      should be a valid TIN per the local jurisdiction
    description: >-
      This validation is only to be applied by the jurisdiction issuing the TIN unless
      a TIN validation tool is available.

  - number: 70005
    targets:
      - //globe:TIN
    rule: >-
      Attributes issuedBy and TypeofTIN must always be present, except that issued By
      is not required when TypeOfTIN is GIR3003 or GIR3004.
    description: >-
      Attributes issuedBy and TypeofTIN must always be present, except that issuedBy
      is not required when TypeOfTIN is GIR3003 or GIR3004.

  - number: 70006
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:UPE/globe:ExcludedUPE/globe:ID/globe:TIN
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:UPE/globe:OtherUPE/globe:ID/globe:TIN
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:ID/globe:TIN
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:QIIR/globe:Exception/globe:TIN
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:CEComputation/globe:Elections/globe:AggregatedReporting/globe:TaxConsolGroupTIN
    references:
      - GeneralSection/CorporateStructure/UPE/ExcludedUPE/ID/TIN
      - GeneralSection/CorporateStructure/UPE/OtherUPE/ID/TIN
      - GeneralSection/CorporateStructure/CE/ID/TIN (except when GloBEStatus contains the value of GIR316 or GIR318)
      - GeneralSection/CorporateStructure/CE/QIIR/Exception/TIN
      - JurisdictionSection/GLoBETax/ETR/ETRStatus/ETRComputation/CEComputation/Elections/AggregatedReporting/TaxConsolGroupTIN
    element: TypeofTIN
    rule: >-
      The following TIN elements must not be completed using TypeofTIN GIR3004 and
      must not be flagged Unknown:
    description: >-
      The following TIN elements must not be completed using TypeofTIN GIR3004 and
      must not be flagged Unknown:
      General Section/Corporate Structure/UPE/ExcludedUPE/ID/TIN
      General Section/Corporate Structure/UPE/OtherUPE/ID/TIN
      General Section/CorporateStructure/CE/ID/TIN (except when GloBEStatus contains
      the value of GIR316 or GIR318)
      General Section/CorporateStructure/CE/QIIR/Exception/TIN
      Jurisdiction
      Section/GLOBETax/ETR/ETRStatus/ETRComputation/CEComputation/Elections/Aggregated
      Reporting/TaxConsolGroup TIN

  - number: 70007
    targets:
      - //globe:TIN
    element: TIN
    rule: >-
      Where the TypeofTIN entered is GIR3003 a reference must be created in the format
      [P2JJYYYYMMDDCCCXXX where P2 is constant, JJ is the ISO Code of the jurisdiction
      of location of the Constituent Entity, YYYYMMDD refers to the date of creation
      of the reference, CCC is a three letter reference to the company or group, and
      XXX is a unique number for the entity within the GIR e.g. 001, 002, 003 to
      differentiate between multiple entites with designated references in the same
      report]. This reference is superseded only in the case that a government or
      administration issues a tax reference or functional equivalent for the entity.
    description: >-
      Where the TypeofTIN entered is GIR3003 a reference must be created in the format
      [P2JJYYYYMMDDCCCXXX where P2 is constant, JJ is the ISO Code of the jurisdiction
      of location of the Constituent Entity, YYYYMMDD refers to the date of creation
      of the reference, CCC is a three letter reference to the company or group, and
      XXX is a unique number for the entity within the GIR e.g. 001, 002, 003 to
      differentiate between multiple entites with designated references in the same
      report]. This reference is superseded only in the case that a government or
      administration issues a tax reference or functional equivalent for the entity.

  - number: 70008
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:UTPRAttribution/globe:RecJurCode
    in:
      - ancestor::globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:UPE/globe:ExcludedUPE/globe:ID/globe:ResCountryCode
      - ancestor::globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:UPE/globe:OtherUPE/globe:ID/globe:ResCountryCode
      - ancestor::globe:GLOBEBody/globe:JurisdictionSection/globe:JurWithTaxingRights/globe:JurisdictionName
    rule: >-
      The Jurisdiction reported in the RecJurCode must be the UPE jurisdiction or one
      of the Jurisdictions reported in the JurisdictionName element of the repeatable
      JurWith TaxingRights element indicating the names of the jurisdictions that have
      taxing rights in respect of the jurisdiction for which the Jurisdictional
      Section is being prepared.
    description: >-
      The Jurisdiction reported in the RecJurCode must be the UPE jurisdiction or one
      of the jurisdictions reported in the JurisdictionName element of the repeatable
      JurWith TaxingRights element.
    implementation_notes: >-
      The UPE jurisdiction is the ResCountryCode of whichever UPE form is present
      (ExcludedUPE or OtherUPE ID). The JurisdictionName is matched against any
      JurWithTaxingRights in the GLOBEBody's JurisdictionSection(s) rather than a
      single jurisdiction-specific section.

  - number: 70009
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:UPE/globe:ExcludedUPE/globe:ID/globe:GlobeStatus
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:UPE/globe:OtherUPE/globe:ID/globe:GlobeStatus
    notIn:
      - "'GIR305'"
      - "'GIR307'"
      - "'GIR308'"
      - "'GIR309'"
      - "'GIR312'"
      - "'GIR313'"
      - "'GIR314'"
      - "'GIR315'"
      - "'GIR317'"
      - "'GIR318'"
    rule: >-
      The UPE GlobeStatus should not accept the following values: GIR305,
      GIR307, GIR308, GIR309, GIR312, GIR313, GIR314, GIR315, GIR317, GIR318
    description: >-
      An Invalid GloBE Status of the UPE was provided. Please ensure that the
      GloBEStatus of the UPE align with note 1.3.1.6 of the GIR.

  - number: 70010
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:UPE/globe:OtherUPE/globe:ID
    absent: globe:ResCountryCode[2]
    implementation_notes: >-
      The schema allows unbounded ResCountryCode; targeting the ID and asserting
      no second occurrence enforces the "only one value" restriction.
    rule: >-
      Only one value is allowed for the ResCountryCode of the UPE/OtherUPE
    description: >-
      The Filing Constituent Entity shall report the 2-character alphabetic country
      code based on the ISO 3166-1 Alpha 2 standard for the jurisdiction where the
      UPE/OtherUPE is located for GloBE purposes.

  - number: 70011
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:ID
    absent: globe:ResCountryCode[2]
    implementation_notes: >-
      The schema allows unbounded ResCountryCode; targeting the ID and asserting
      no second occurrence enforces the "only one value" restriction.
    rule: >-
      Only one value is allowed for the ResCountryCode of the CE
    description: >-
      The Filing Constituent Entity shall report the 2-character alphabetic country
      code based on the ISO 3166-1 Alpha 2 standard for the jurisdiction where the CE
      is located for GloBE purposes.

  - number: 70012
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:UPE/globe:ExcludedUPE/globe:ID/globe:Rules
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:UPE/globe:OtherUPE/globe:ID/globe:Rules
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:ID/globe:Rules
    element: Rules
    rule: >-
      All Entities (i.e. ExcludedUPE, OtherUPE or CEs) with residence in same
      Jurisdiction (same ResCountryCode) must have same Rules, unless the Rules
      element contains the Value GIR204 (QDMTT).
    description: >-
      The 'rules' should be completed in accordance with GIR Note 1.3.2.1.3. Where the
      rules apply to all entities with residence in the same jurisdiction, the Rules
      Value should be the same. The rules value might be different where the QDMTT in
      that jurisdiction treats certain entities differently (e.g. where the QDMTT does
      not apply to investment entities, meaning that where GloBEStatus = GIR310, the
      rules value of GIR204 might not be an exact match.

  - number: 70013
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:ID/globe:GlobeStatus
    when: . = 'GIR313'
    absent: ../globe:GlobeStatus[. = 'GIR314']
    rule: >-
      When the GlobeStatus contains the value of GIR313, the value of GIR314 must not
      also be reported for the same CE
    description: >-
      A CE element has been completed with a GloBE Status which indicates that an
      entity is both a JV and a JV Subsidiary, please ensure that the GloBEStatus of
      the same CE does not contain GIR313 and GIR314

  - number: 70014
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:ID/globe:GlobeStatus
    when: . = 'GIR307'
    absent: ../globe:GlobeStatus[. = 'GIR308']
    rule: >-
      When the GlobeStatus contains the value of GIR307, the value of GIR308 must not
      also be reported for the same CE
    description: >-
      A CE has been identified as having the Globe Status of both a Minority-Owned
      Parent Entity and a Minority-Owned Subsidiary, please ensure that the
      GlobeStatus of the same CE does not contain GIR307 and GIR308

  - number: 70015
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE
    when: globe:ID/globe:GlobeStatus = 'GIR308'
    present: (preceding-sibling::globe:CE | following-sibling::globe:CE)/globe:ID/globe:GlobeStatus[. = 'GIR307']
    implementation_notes: >-
      Targets each CE carrying GIR308 and looks for GIR307 on its sibling CEs.
      The sibling axes exclude the target itself, so "another CE" is enforced.
    rule: >-
      When the GlobeStatus contains the value of GIR308, then there must be another CE
      in the corporate structure which contains the value of GIR307 (i.e. when
      CE/ID/GlobeStatus = GIR308, must also be a separate CE element which contains
      CE/ID/GlobeStatus=GIR307)
    description: >-
      A Minority-Owned Subsidiary has been identified in the Corporate Structue and
      there is no other CE reported in the corporate Structure which is a
      Minority-Owned Parent Entity.

  - number: 70016
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:ID/globe:GlobeStatus
    when: . = 'GIR307'
    present: ../globe:GlobeStatus[. = 'GIR309']
    rule: >-
      When the GlobeStatus contains the value of GIR307, the value of GIR309 should
      also be reported in the GlobeStatus
    description: >-
      A Minority-Owned Parent Entity must also be a Minority-Owned Constituent Entity,
      as a result when the value of GIR307 is selected for the GlobeStatus then the
      value of GIR309 must also be provided.

  - number: 70017
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:ID/globe:GlobeStatus
    when: . = 'GIR308'
    present: ../globe:GlobeStatus[. = 'GIR309']
    rule: >-
      When the GlobeStatus contains the value of GIR308, the value of GIR309 should
      also be reported in the GlobeStatus
    description: >-
      A Minority-Owned Subsidiary must also be a Minority-Owned Constituent Entity, as
      a result when GIR308 is selected for the GlobeStatus then the value of GIR309
      must also be selected

  - number: 70018
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:ID/globe:GlobeStatus
    when: . = 'GIR305'
    absent: ../globe:GlobeStatus[. = 'GIR306']
    rule: >-
      When the GlobeStatus contains the value of GIR305, the value of GIR306 must not
      also be reported for the same CE
    description: >-
      A CE has been identified as having the GlobeStatus of both a Permanent
      Establishment and a Main entity, whereas the same CE cannot have the Globe
      Status of PE and ME.

  - number: 70019
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE
    when: globe:ID/globe:GlobeStatus = 'GIR305'
    present: (preceding-sibling::globe:CE | following-sibling::globe:CE)/globe:ID/globe:GlobeStatus[. = 'GIR306']
    implementation_notes: >-
      Same shape as 70015 (GIR308 -> GIR307): targets each CE carrying GIR305 and
      looks for GIR306 on its sibling CEs. The sibling axes exclude the target
      itself, so the rule enforces "another CE".
    rule: >-
      When the GlobeStatus contains the value of GIR305, then there must be another CE
      which contains the GlobeStatus of GIR306 (i.e. when CE/ID/GlobeStatus = GIR305,
      there must also be a separate CE element which contains CE/ID/GlobeStatus =
      GIR306)
    description: >-
      A permanent establishment has been reported in the Corporate Structure of the
      GIR, and no main entity has been reported.

  - number: 70020
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:ID/globe:GlobeStatus
    when: . = 'GIR316' or . = 'GIR318'
    absent: ../globe:GlobeStatus[2]
    implementation_notes: >-
      The schema allows unbounded GlobeStatus; once one carries GIR316/GIR318,
      asserting no second occurrence enforces the "no other value" restriction.
    rule: >-
      If the GlobeStatus contains the value of GIR316 or GIR318, then there should be
      no other value in the GlobeStatus (if GlobeStatus = GIR316 - GlobeStatus can
      only contain the value of GIR316 and if GlobeStatus = GIR318 - GlobeStatus can
      only contain the value of GIR318)
    description: >-
      A NonGroup -member or excluded entity was reported in the GlobeStatus and at
      least one additional GlobeStatus was reported. When the CE element has the
      GlobeStatus of GIR316 or GIR318, it can only contain the single value in the
      GlobeStatus (i.e. only GIR316 or only GIR318)

  - number: 70021
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE
    when: globe:ID/globe:GlobeStatus = 'GIR316' or globe:ID/globe:GlobeStatus = 'GIR318'
    present: globe:OwnershipChange
    implementation_notes: >-
      Targets the CE (the common parent of its GlobeStatus and OwnershipChange) so both
      operands are plain child paths. The rule's TIN-match parenthetical only re-states
      that the OwnershipChange belongs to the same CE, which the nesting already
      guarantees, so a presence check on the CE's own OwnershipChange suffices.
    rule: >-
      The GlobeStatus element cannot contain the value of GIR316 or GIR318, unless
      there is a completed OwnershipChange for the CE element (i.e., when Corporate
      Structure/CE/ID/TIN matches to Corporate
      Structure/CE/OwnershipChange/PreOwnership/TIN).
    description: >-
      The GlobeStatus of a CE can only contain the value of 'Excluded Entity' or
      'NonGroup-Member' when there has been a change in the GlobeStatus of the
      reported CE. Therefore, there must be a completed OwnershipChange element which
      indicates the GlobeStatus before the entity became an Excluded Entity or a
      'Non-Group Member'

  - number: 70022
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:OwnershipChange/globe:ChangeDate
    atLeast:
      expected: ancestor::globe:GLOBEBody/globe:FilingInfo/globe:Period/globe:Start
      type: date
    rule: >-
      The ChangeDate must not be a date which takes place before the Period Start date
      as reported in the Filing Info
    description: >-
      An ownership change has a change date which takes place before the reporting
      fiscal year
    implementation_notes: >-
      Period Start is reached via ancestor::globe:GLOBEBody so that in a
      multi-filing message each ChangeDate is checked against its own filing's
      period.

  - number: 70023
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:OwnershipChange/globe:ChangeDate
    atMost:
      expected: ancestor::globe:GLOBEBody/globe:FilingInfo/globe:Period/globe:End
      type: date
    rule: >-
      The ChangeDate must not be a date which takes place after the Period End date as
      reported in the Filing Info
    description: >-
      An ownership change has a change date which takes place after the reporting
      fiscal year
    implementation_notes: >-
      Mirror of 70022 against Period End (same ancestor::globe:GLOBEBody scoping).

  - number: 70024
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:OwnershipChange
    when: globe:PreGlobeStatus = 'GIR719'
    absent: globe:PreOwnership
    implementation_notes: >-
      Targets the common OwnershipChange ancestor (not the PreOwnership the spec
      cites) since the rule asserts that element's absence. PreGlobeStatus is
      repeatable, so the guard's node-set `=` fires when any value is GIR719.
    rule: >-
      The PreOwnership must not be completed when the PreGlobeStatus has the value of
      GIR719
    description: >-
      Where an entity was an excluded entity before the change, then the PreOwnership
      element should not be completed.

  - number: 70025
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:OwnershipChange/globe:PreOwnership/globe:TIN
    when: ../globe:OwnershipType = 'GIR805' or ../globe:OwnershipType = 'GIR806'
    allOf:
      - equals:
          expected: "'NOTIN'"
      - equals:
          actual: "@TypeOfTIN"
          expected: "'GIR3004'"
    rule: >-
      When the Ownership Type contains the value of GIR805 or GIR806, then the
      TypeOfTIN must contain the value GIR3004 and the TIN must contain the text,
      'NOTIN'
    description: >-
      When the Ownership Type indicates that the ownership interests are held in the
      aggregate by Non-Group Members or Excluded Entities, the TIN should not reported
      and as such the TypeOfTIN must be GIR3004.

  - number: 70026
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:Ownership/globe:OwnershipPercentage
    when: ancestor::globe:CE[1]/globe:ID/globe:GlobeStatus = 'GIR305'
    equals:
      expected: 1
    rule: >-
      When the GloBEStatus contains the value of GIR305, then the Ownership Percentage
      must equal 100%
    description: >-
      The ownership percentage of a PE must equal 100%

  - number: 70027
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:Ownership
    when: parent::globe:CE/globe:ID/globe:GlobeStatus = 'GIR318'
    allOf:
      - equals:
          actual: globe:OwnershipPercentage
          expected: 0
      - equals:
          actual: globe:TIN
          expected: "'NOTIN'"
      - equals:
          actual: globe:OwnershipType
          expected: "'GIR806'"
    rule: >-
      When the GloBEStatus contains the value of GIR318, then the OwnershipPercentage
      should have the value of 0%, the TIN value should be NOTIN and OwnershipType
      should be GIR806.
    description: >-
      When the GloBEStatus is a 'NonGroup-Member' as a result of completion of the
      OwnershipChange element, then Ownershipercentage as reported in the
      OwnershipElement should be 0%
    implementation_notes: >-
      This specification defines 3 conditions on three different elements which does not quite fit
      in the standard format of the validation rules. Therefore we combined the 3 target path
      into a single one which selects the common parent element.
      
  - number: 70028
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:Ownership/globe:OwnershipPercentage
    when: not(ancestor::globe:CE[1]/globe:ID/globe:GlobeStatus = 'GIR318')
    notEquals:
      expected: 0
    rule: >-
      Unless the GloBEStatus contains the value of GIR318, then the
      OwnershipPercentage must not be 0%
    description: >-
      The Ownership Percentage should not be 0% unless the reported ownership
      percentage is in respect of a Non-Group Member.

  - number: 70029
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:Ownership/globe:TIN
    references:
      - GLOBEBody/GeneralSection/CorporateStructure/CE/Ownership/OwnershipType
      - GLOBEBody/GeneralSection/CorporateStructure/UPE/ExcludedUPE/ID/TIN
      - GLOBEBody/GeneralSection/CorporateStructure/UPE/OtherUPE/ID/TIN
    element: TIN
    rule: >-
      When the Ownership Type contains the value of GIR801, then the Ownership/TIN
      provided in the Ownership element must match at least one of the TINs that was
      reported in either the OtherUPE/ID/TIN or ExcludedUPE/ID/TIN
    description: >-
      The ownership type was identified as being the UPE, the TIN of the UPE provided
      at label 1.3.2.1.9 does match the TIN reported at 1.3.1.4. Please ensure that
      the ownership TIN matches to a TIN reported in the corporate structure.

  - number: 70030
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:Ownership/globe:TIN
    references:
      - GLOBEBody/GeneralSection/CorporateStructure/CE/Ownership/OwnershipType
      - GLOBEBody/GeneralSection/CorporateStructure/CE/ID/TIN
    element: TIN
    rule: >-
      When the Ownership Type contains the value of GIR802, GIR803 or GIR804, then the
      Ownership/TIN provided in the Ownership element must match to a TIN that was
      reported in the CE element via the ID/TIN.
    description: >-
      The ownership type was identified as being a CE, JV or JV Subsidiary, the TIN
      provided at label 1.3.2.1.9 does not match to a TIN reported in the corporate
      structure at 1.3.2.1.5. Please ensure that the ownership TIN matches to a TIN
      reported in the corporate structure.

  - number: 70031
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:Ownership/globe:TIN
    references:
      - GLOBEBody/GeneralSection/CorporateStructure/CE/ID/GlobeStatus
      - GLOBEBody/GeneralSection/CorporateStructure/CE/ID/TIN
    element: TIN
    rule: >-
      When the Corporate Structure/CE/ID/GlobeStatus contains the value of GIR305,
      then the Ownership/TIN must be equal at least one TIN of the entity which
      reported the GlobeStatus of GIR306 either in Corporate
      Structure/CE/ID/GlobeStatus, or CorporateStructure/UPE/OtherUPE/ID/GlobeStatus.
    description: >-
      The ownership TIN of a CE identified as being a PE is not held by a CE which is
      a main entity

  - number: 70032
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:QIIR
    present: ../globe:ID/globe:Rules[. = 'GIR201' or . = 'GIR202']
    rule: >-
      When the QIIR element is provided, then the Rules element as provided in the CE
      element must contain either GIR201 or GIR202
    description: >-
      The QIIR element has been provided for a CE which is in a jurisdiction which
      does not have a QIIR selected in the Rules element.

  - number: 70033
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:QIIR/globe:Exception/globe:TIN
    references:
      - GLOBEBody/GeneralSection/CorporateStructure/CE/ID/TIN
    element: TIN
    rule: >-
      The TIN provided must match with a TIN reported for any other CE in the
      CorporateStructure.
    description: >-
      The TIN provided to identify either the POPE or IPE does not match with an
      existing TIN in the corporate structure

  - number: 70034
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:QIIR
    when: globe:POPE-IPE = 'GIR902' and globe:Exception
    isTrue: globe:Exception/globe:ExceptionRule/globe:Art2.1.3
    implementation_notes: >-
      Targets the common QIIR ancestor (not the Art2.1.3 leaf the spec cites) so
      the guard requires a completed Exception explicitly and the consequence
      still fails when GIR902 selects Art2.1.5 instead (Art2.1.3 then missing).
      POPE-IPE carries the bare enum value 'GIR902' (the spec's 'GIR902-IPE'
      label appends the #9 enumeration's "IPE" description).
    rule: >-
      If the POPE-IPE element has the value of 'GIR902-IPE' and the Exception element
      is completed because an exception applies, the Art.2.1.3 element must have the
      status of TRUE
    description: >-
      The exception under article 2.1.3 should be selected in relation to identifying
      another IPE which is required to apply the QIIR

  - number: 70035
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:CE/globe:QIIR
    when: globe:POPE-IPE = 'GIR901' and globe:Exception
    isTrue: globe:Exception/globe:ExceptionRule/globe:Art2.1.5
    implementation_notes: >-
      Targets the common QIIR ancestor (not the Art2.1.5 leaf the spec cites) so
      the guard requires a completed Exception explicitly and the consequence
      still fails when GIR901 selects Art2.1.3 instead (Art2.1.5 then missing).
      POPE-IPE carries the bare enum value 'GIR901' (the spec's 'GIR901-POPE'
      label appends the #9 enumeration's "POPE" description).
    rule: >-
      If the POPE-IPE element has the value of 'GIR901-POPE' and the Exception element
      is completed because an exception applies, the Art.2.1.5 element must have the
      status of TRUE
    description: >-
      The exception under article 2.1.5 should be selected in relation to identifying
      another POPE which is required to apply the QIIR

  - number: 70036
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:Summary/globe:Jurisdiction/globe:Subgroup
    rule: >-
      If there is more than one Subgroup per Jurisdiction the whole Summary must be
      repeated. The subgroup element can be different as the computation can be
      different for each subgroup.
    description: >-
      If more than one Subgroup element is provided, then the number of Subgroup
      elements must match the number of Summary sections provided for that
      Jurisdiction. For example, if there are 3 Subgroup elements reported for a
      jurisdiction, then there must be 3 Summary sections for that jurisdiction, each
      one with 3 reported Subgroup elements.

  - number: 70037
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:SubGroup/globe:TIN
    references:
      - GLOBEBody/Summary/Jurisdiction/Subgroup/TIN
      - GLOBEBody/JurisdictionSection/Jurisdiction
    element: Subgroup
    rule: >-
      Where a subgroup element is completed in the 'Summary', then Subgroup element in
      the 'JurisdictionSection' should also be completed. The TIN must match to the
      corresponding TIN in the JurisdictionSection /GLOBETax/ETR/SubGroup. Rule only
      applicable where a JurisdictionSection which relates to the same 'Jurisdiction'
      of the Summary is provided (i.e., where Summary/Jurisdiction/JurisdictionName =
      JurisdictionSection/Jurisdiction).
    description: >-
      The summary section for a jurisdiction indicates that there is a subgroup with a
      separate ETR or Top-up Tax computation, while for the same jurisdiction in the
      Jurisdiction Section, no separate set of subgroup calculations has been
      provided.

  - number: 70038
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:Summary/globe:SafeHarbour
    when: >-
      translate(ancestor::globe:GLOBE_OECD/globe:MessageSpec/globe:ReportingPeriod, '-', '') > 20280630
      or
      translate(ancestor::globe:GLOBEBody/globe:FilingInfo/globe:Period/globe:End, '-', '') > 20280630
    notIn:
      - "'GIR1203'"
      - "'GIR1204'"
      - "'GIR1205'"
    implementation_notes: >-
      Either the MessageSpec ReportingPeriod or the FilingInfo Period/End being later
      than the 30/06/2028 transitional cut-off triggers the restriction, so the guard
      ORs both. Both are YYYY-MM-DD, so translate() strips the dashes to compare them as
      integers against 20280630 (a missing date becomes NaN and is silently
      non-triggering; the cut-off date itself is not "later", so the compare is strict).
      The forbidden inputs are checked with notIn against the target SafeHarbour value.
    rule: >-
      When the Reporting Period or Period End Date is later than 30/06/2028, then the
      SafeHarbour element cannot have the input of GIR1203, GIR1204 or GIR1205.
    description: >-
      The Transitional CbCR Safe Harbour has been selected for a reporting fiscal year
      that is beyond the transitional period

  - number: 70039
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:Summary/globe:SafeHarbour
    when: >-
      translate(ancestor::globe:GLOBE_OECD/globe:MessageSpec/globe:ReportingPeriod, '-', '') > 20261231
      or
      translate(ancestor::globe:GLOBEBody/globe:FilingInfo/globe:Period/globe:End, '-', '') > 20261231
    notEquals:
      expected: "'GIR1206'"
    implementation_notes: >-
      Same shape as 70038 with the UTPR Safe Harbour cut-off of 31/12/2026: either the
      MessageSpec ReportingPeriod or the FilingInfo Period/End being later triggers the
      restriction, translate() strips the YYYY-MM-DD dashes for an integer compare
      against 20261231 (strict, so the cut-off date itself is allowed). A single
      forbidden input reads better as notEquals than a one-element notIn.
    rule: >-
      When the Reporting Period or Period End Date is later than 31/12/2026, then the
      SafeHarbour element cannot have the input of GIR1206
    description: >-
      The Transitional UTPR Safe Harbour has been selected for a reporting fiscal year
      that is beyond the transitional period.

  - number: 70040
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:Summary/globe:SafeHarbour
    when: . = 'GIR1206'
    equals:
      actual: ../globe:Jurisdiction/globe:JurisdictionName
      expected: >-
        ancestor::globe:GLOBEBody/globe:GeneralSection/globe:CorporateStructure/globe:UPE/*/globe:ID/globe:ResCountryCode
    implementation_notes: >-
      GIR1206 is only valid in the UPE jurisdiction, so when the target SafeHarbour is
      GIR1206 the Summary's Jurisdiction/JurisdictionName must equal a UPE ResCountryCode.
      The UPE ID sits under either ExcludedUPE or OtherUPE, so the path wildcards that
      choice (UPE/*/ID/ResCountryCode); equals is an existential node-set match, so any
      one UPE jurisdiction satisfies it.
    rule: >-
      The SafeHarbour element can only have the input of GIR1206 in the UPE
      jurisdiction. (i.e., Summary/Jurisdiction/JurisdictionName must be equal to the
      ResCountryCode reported in the UPE ID, so equal to Corporate
      Structure/UPE/OtherUPE/ID/ResCountryCode)
    description: >-
      The Transitional UTPR Safe Harbour has been applied in a jurisdiction which is
      not the UPE Jurisdiction

  - number: 70041
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:Summary/globe:SafeHarbour
    when: >-
      ancestor::globe:GLOBEBody/globe:FilingInfo/globe:AccountingInfo/globe:CFSofUPE = 'GIR502'
      or
      ancestor::globe:GLOBEBody/globe:FilingInfo/globe:AccountingInfo/globe:CFSofUPE = 'GIR504'
    notIn:
      - "'GIR1207'"
      - "'GIR1208'"
      - "'GIR1209'"
    implementation_notes: >-
      The NMCE Simplified Calculations (GIR1207/1208/1209) are only available when the
      UPE prepares qualifying Consolidated Financial Statements, i.e. CFSofUPE is not
      GIR502 or GIR504. The guard reaches the FilingInfo AccountingInfo CFSofUPE and
      notIn forbids those three inputs on the target SafeHarbour value.
    rule: >-
      When the FilingInfo CFSofUPE has the input of GIR502 or GIR504, then SafeHarbour
      element cannot have the input of GIR1207, GIR1208 , or GIR1209.
    description: >-
      The Non-material Constituent Entity (NMCE) Simplified Calculations can only be
      completed where the UPE prepares Consolidated Financial Statements as defined by
      paragraphs (a) and (c) of the Consolidated Financial Statements definition in
      Article 10.1.1

  - number: 70042
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:Summary
    when: >-
      globe:JurWithTaxingRights and
      (not(globe:SafeHarbour) or count(globe:SafeHarbour[. != 'GIR1206']) = 0)
    allOf:
      - present: globe:ETRRange
      - present: globe:SBIE
      - present: globe:QDMTTut
      - present: globe:GLoBETut
    rule: >-
      If the JurWithTaxingRights element is completed, and the Safe Harbour element is
      not completed or only has the value of GIR1206, then the elements ETRRange,
      SBIE, QDMTTut and GloBETut must be completed
    description: >-
      If one or several jurisdictions have taxing rights, and no Safe Harbour is
      applied or only the UTPR Safe Harbour is applied, then all other elements of the
      summary section must be completed

  - number: 70043
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:Summary
    when: globe:JurWithTaxingRights and globe:SafeHarbour = 'GIR1202'
    allOf:
      - present: globe:ETRRange
      - present: globe:SBIE
      - present: globe:QDMTTut
    rule: >-
      If the JurWithTaxingRights element is completed, and the Safe Harbour element
      has the value of GIR1202, then the elements ETRRange, SBIE and QDMTTut must be
      completed
    description: >-
      As a QDMTT SafeHarbour has been applied for the jurisdiction, then all other
      elements except GloBETut of the summary section must be completed

  - number: 70044
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus
    present: globe:ETRException | globe:ETRComputation
    rule: >-
      When the ETRStatus is completed, it must contain at least one of ETRException or
      ETRComputation.
    description: >-
      Part 2 or Part 3 of the GIR must be completed

  - number: 70045
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR
    when: >-
      ancestor::globe:GLOBEBody/globe:Summary[(globe:SafeHarbour = 'GIR1203' or
      globe:SafeHarbour = 'GIR1204' or globe:SafeHarbour =
      'GIR1205')][not(globe:Jurisdiction/globe:Subgroup)]/globe:Jurisdiction/globe:JurisdictionName
      = ancestor::globe:JurisdictionSection/globe:Jurisdiction
      or
      ancestor::globe:GLOBEBody/globe:Summary[(globe:SafeHarbour = 'GIR1203' or
      globe:SafeHarbour = 'GIR1204' or globe:SafeHarbour =
      'GIR1205')][globe:Jurisdiction/globe:Subgroup]/globe:Jurisdiction/globe:Subgroup/globe:TIN
      = globe:SubGroup/globe:TIN
    present: globe:ETRStatus/globe:ETRException/globe:TransitionalCbCRSafeHarbour
    implementation_notes: >-
      The SafeHarbour input lives in the Summary while the dependent element lives in
      the JurisdictionSection, so the target is the ETR (a stable context with a usable
      Jurisdiction and SubGroup/TIN) and the guard correlates back to the Summary by
      existential node-set equality. The spec's two-level key becomes two clauses: a
      Summary carrying the input code with no Subgroup matches by jurisdiction
      (Summary/Jurisdiction/JurisdictionName = JurisdictionSection/Jurisdiction); a
      Summary with a Subgroup matches by TIN against this ETR's own SubGroup/TIN
      (Summary/Jurisdiction/Subgroup/TIN = ETR/SubGroup/TIN). Comparing the TIN against
      this specific ETR pins the jurisdiction too, assuming subgroup TINs are unique
      across jurisdictions. See specs/cross-section-correlation-design.md.
    rule: >-
      When the SafeHarbour element has the input of GIR1203, GIR1204 or GIR1205 for a
      jurisdiction (or for a subgroup), then the TransitionalCbCRSafeHarbour element
      for that jurisdiction (or for that subgroup) must be completed (i.e., where the
      Summary/Jurisdiction/JurisdictionName = Jurisdiction Section/Jurisdiction, or if
      Summary/Jurisdiction/Subgroup is completed, then also match where
      Summary/Jurisdiction/Subgroup/TIN = Jurisdiction
      Section/GLoBETax/ETR/SubGroup/TIN)
    description: >-
      A Transitional CbCR Safe Harbour has been selected in the summary section of the
      GIR, but no corresponding TransitionalCbCR element with the relevant CbCR
      calculations for the Tested Jurisdiction has been provided

  - number: 70046
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR
    when: globe:ETRStatus/globe:ETRException/globe:TransitionalCbCRSafeHarbour
    present: globe:SubGroup/globe:TypeofSubGroup[. = 'GIR1607' or . = 'GIR1608']
    rule: >-
      When the TransitionalCbCRSafeHarbour is completed for a jurisdiction, then
      SubGroup element of the ETR must be completed, the TypeofSubGroup must contain
      either GIR1607 or GIR1608.
    description: >-
      The TypeofSubGroup element must be completed and it cannot contain a value other
      than GIR1607 or GIR1608.

  - number: 70047
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR
    when: >-
      ancestor::globe:GLOBEBody/globe:Summary[globe:SafeHarbour =
      'GIR1203'][not(globe:Jurisdiction/globe:Subgroup)]/globe:Jurisdiction/globe:JurisdictionName
      = ancestor::globe:JurisdictionSection/globe:Jurisdiction
      or
      ancestor::globe:GLOBEBody/globe:Summary[globe:SafeHarbour =
      'GIR1203'][globe:Jurisdiction/globe:Subgroup]/globe:Jurisdiction/globe:Subgroup/globe:TIN
      = globe:SubGroup/globe:TIN
    present: globe:ETRStatus/globe:ETRException/globe:TransitionalCbCRSafeHarbour/globe:Revenue
    implementation_notes: >-
      Targets the ETR and correlates back to the Summary by the two-clause existential
      match described in rule 70045 (jurisdiction for non-subgroup Summaries, SubGroup/
      TIN for subgroup Summaries), keyed on the GIR1203 input.
    rule: >-
      When the SafeHarbour element has the input of GIR1203 for a jurisdiction (or for
      a subgroup), then the revenue element must be completed for that jurisdiction
      (or for that subgroup) (i.e., where the Summary/Jurisdiction/JurisdictionName =
      JurisdictionSection/Jurisdiction, or if Summary/Jurisdiction/Subgroup is
      completed, then also match where Summary/Jurisdiction/Subgroup/TIN =
      JurisdictionSection/GLOBETax/ETR/SubGroup/TIN)
    description: >-
      As the Deminimis CbCR safe harbour has been completed, the Revenue element for
      the tested jurisdiction must also be provided

  - number: 70048
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR
    when: >-
      ancestor::globe:GLOBEBody/globe:Summary[globe:SafeHarbour =
      'GIR1204'][not(globe:Jurisdiction/globe:Subgroup)]/globe:Jurisdiction/globe:JurisdictionName
      = ancestor::globe:JurisdictionSection/globe:Jurisdiction
      or
      ancestor::globe:GLOBEBody/globe:Summary[globe:SafeHarbour =
      'GIR1204'][globe:Jurisdiction/globe:Subgroup]/globe:Jurisdiction/globe:Subgroup/globe:TIN
      = globe:SubGroup/globe:TIN
    present: globe:ETRStatus/globe:ETRException/globe:TransitionalCbCRSafeHarbour/globe:IncomeTax
    implementation_notes: >-
      Targets the ETR and correlates back to the Summary by the two-clause existential
      match described in rule 70045 (jurisdiction for non-subgroup Summaries, SubGroup/
      TIN for subgroup Summaries), keyed on the GIR1204 input.
    rule: >-
      When the SafeHarbour element has the input of GIR1204 for a jurisdiction (or for
      a subgroup), then the IncomeTax element must be completed for that jurisdiction
      (or for that subgroup) (i.e., where the Summary/Jurisdiction/JurisdictionName =
      JurisdictionSection/Jurisdiction, or if Summary/Jurisdiction/Subgroup is
      completed, then also match where Summary/Jurisdiction/Subgroup/TIN =
      JurisdictionSection/GLOBETax/ETR/SubGroup/TIN)
    description: >-
      As the Simplified ETR Test has been selected for the Tested Jurisdiction, the
      IncomeTax element must also be provided.

  - number: 70049
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR
    when: >-
      ancestor::globe:GLOBEBody/globe:Summary[globe:SafeHarbour =
      'GIR1206'][not(globe:Jurisdiction/globe:Subgroup)]/globe:Jurisdiction/globe:JurisdictionName
      = ancestor::globe:JurisdictionSection/globe:Jurisdiction
      or
      ancestor::globe:GLOBEBody/globe:Summary[globe:SafeHarbour =
      'GIR1206'][globe:Jurisdiction/globe:Subgroup]/globe:Jurisdiction/globe:Subgroup/globe:TIN
      = globe:SubGroup/globe:TIN
    present: globe:ETRStatus/globe:ETRException/globe:UTPRSafeHarbour
    implementation_notes: >-
      Targets the ETR and correlates back to the Summary by the two-clause existential
      match described in rule 70045 (jurisdiction for non-subgroup Summaries, SubGroup/
      TIN for subgroup Summaries), keyed on the GIR1206 input.
    rule: >-
      When the SafeHarbour element has the input of GIR1206 for a jurisdiction (or for
      a subgroup), then the UTPRSafeHarbour element must be completed for that
      jurisdiction (or for that subgroup). (i.e., where the
      Summary/Jurisdiction/JurisdictionName = JurisdictionSection/Jurisdiction, or if
      Summary/Jurisdiction/Subgroup is completed, then also match where
      Summary/Jurisdiction/Subgroup/TIN =
      JurisdictionSection/GLOBETax/ETR/SubGroup/TIN)
    description: >-
      The UTPR safe harbour has been selected for a jurisdiction, therefore the
      UTPRSafeHarbour element must be completed and the CIT rate provided.

  - number: 70050
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR
    when: >-
      ancestor::globe:GLOBEBody/globe:Summary[(globe:SafeHarbour = 'GIR1207' or
      globe:SafeHarbour = 'GIR1208' or globe:SafeHarbour =
      'GIR1209')][not(globe:Jurisdiction/globe:Subgroup)]/globe:Jurisdiction/globe:JurisdictionName
      = ancestor::globe:JurisdictionSection/globe:Jurisdiction
      or
      ancestor::globe:GLOBEBody/globe:Summary[(globe:SafeHarbour = 'GIR1207' or
      globe:SafeHarbour = 'GIR1208' or globe:SafeHarbour =
      'GIR1209')][globe:Jurisdiction/globe:Subgroup]/globe:Jurisdiction/globe:Subgroup/globe:TIN
      = globe:SubGroup/globe:TIN
    present: globe:ETRStatus/globe:ETRComputation/globe:Non-MaterialCE
    implementation_notes: >-
      Targets the ETR and correlates back to the Summary by the two-clause existential
      match described in rule 70045 (jurisdiction for non-subgroup Summaries, SubGroup/
      TIN for subgroup Summaries), keyed on the GIR1207, GIR1208 or GIR1209 input.
    rule: >-
      When the SafeHarbour element has the input of GIR1207, GIR1208 or GIR1209 for a
      jurisdiction (or for a subgroup), then the Non-MaterialCE element must be
      completed for that jurisdiction (or for that subgroup) (i.e., where the
      Summary/Jurisdiction/JurisdictionName = Jurisdiction Section/Jurisdiction, or if
      Summary/Jurisdiction/Subgroup is completed, then also match where
      Summary/Jurisdiction/Subgroup/TIN =
      JurisdictionSection/GLoBETax/ETR/SubGroup/TIN)
    description: >-
      When the SafeHarbour element has the input of GIR1207, GIR1208 or GIR1209 for a
      jurisdiction (or for a subgroup), then the Non-MaterialCE element must be
      completed for that jurisdiction (or for that subgroup)

  - number: 70051
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR
    when: >-
      ancestor::globe:GLOBEBody/globe:Summary[globe:SafeHarbour =
      'GIR1208'][not(globe:Jurisdiction/globe:Subgroup)]/globe:Jurisdiction/globe:JurisdictionName
      = ancestor::globe:JurisdictionSection/globe:Jurisdiction
      or
      ancestor::globe:GLOBEBody/globe:Summary[globe:SafeHarbour =
      'GIR1208'][globe:Jurisdiction/globe:Subgroup]/globe:Jurisdiction/globe:Subgroup/globe:TIN
      = globe:SubGroup/globe:TIN
    present: globe:ETRStatus/globe:ETRComputation/globe:Non-MaterialCE/globe:RFY/globe:AggregateSimplified
    implementation_notes: >-
      Targets the ETR and correlates back to the Summary by the two-clause existential
      match described in rule 70045 (jurisdiction for non-subgroup Summaries, SubGroup/
      TIN for subgroup Summaries), keyed on the GIR1208 input.
    rule: >-
      When the SafeHarbour element has the input of GIR1208 for a jurisdiction (or for
      a subgroup), then the Aggregate Simplified element must be completed for that
      jurisdiction (or for that subgroup) (i.e., where the
      Summary/Jurisdiction/JurisdictionName = JurisdictionSection/Jurisdiction, or if
      Summary/Jurisdiction/Subgroup is completed, then also match where
      Summary/Jurisdiction/Subgroup/TIN =
      JurisdictionSection/GLOBETax/ETR/SubGroup/TIN)
    description: >-
      When the SafeHarbour element has the input of GIR1208 for a jurisdiction (or for
      a subgroup), then the AggregateSimplified element must be completed for that
      jurisdiction (or for that subgroup)

  - number: 70052
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR
    when: >-
      ancestor::globe:GLOBEBody/globe:Summary[globe:SafeHarbour =
      'GIR1209'][not(globe:Jurisdiction/globe:Subgroup)]/globe:Jurisdiction/globe:JurisdictionName
      = ancestor::globe:JurisdictionSection/globe:Jurisdiction
      or
      ancestor::globe:GLOBEBody/globe:Summary[globe:SafeHarbour =
      'GIR1209'][globe:Jurisdiction/globe:Subgroup]/globe:Jurisdiction/globe:Subgroup/globe:TIN
      = globe:SubGroup/globe:TIN
    present: globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:SubstanceExclusion
    implementation_notes: >-
      Targets the ETR and correlates back to the Summary by the two-clause existential
      match described in rule 70045 (jurisdiction for non-subgroup Summaries, SubGroup/
      TIN for subgroup Summaries), keyed on the GIR1209 input.
    rule: >-
      When the SafeHarbour element has the input of GIR1209 for a jurisdiction (or for
      a subgroup), then the Substance Exclusion element must be completed for that
      jurisdiction (or for that subgroup) (i.e., where the
      Summary/Jurisdiction/JurisdictionName = JurisdictionSection/Jurisdiction, or if
      Summary/Jurisdiction/Subgroup is completed, then also match where
      Summary/Jurisdiction/Subgroup/TIN =
      JurisdictionSection/GLOBETax/ETR/SubGroup/TIN)
    description: >-
      The Routine Profit Test for NMCEs requires the Substance Exclusion element to be
      completed

  - number: 70053
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR
    when: >-
      (ancestor::globe:GLOBEBody/globe:Summary[globe:SafeHarbour =
      'GIR1205'][not(globe:Jurisdiction/globe:Subgroup)]/globe:Jurisdiction/globe:JurisdictionName
      = ancestor::globe:JurisdictionSection/globe:Jurisdiction
      or
      ancestor::globe:GLOBEBody/globe:Summary[globe:SafeHarbour =
      'GIR1205'][globe:Jurisdiction/globe:Subgroup]/globe:Jurisdiction/globe:Subgroup/globe:TIN
      = globe:SubGroup/globe:TIN) and
      globe:ETRStatus/globe:ETRException/globe:TransitionalCbCRSafeHarbour/globe:Profit > 0
    present: globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:SubstanceExclusion
    implementation_notes: >-
      Targets the ETR and correlates back to the Summary by the two-clause existential
      match described in rule 70045 (jurisdiction for non-subgroup Summaries, SubGroup/
      TIN for subgroup Summaries), keyed on the GIR1205 input. The Profit exception is
      local to the ETR, so the guard also requires this ETR's
      TransitionalCbCRSafeHarbour/Profit to be positive (a Profit of 0 or negative
      leaves the rule vacuous).
    rule: >-
      When the SafeHarbour element has the input of GIR1205 for a jurisdiction (or for
      a subgroup) then the Substance Exclusion element must be completed for that
      jurisdiction (or for a subgroup). Unless the Profit element is either 0 or a
      negative value (i.e., where the Summary/Jurisdiction/JurisdictionName =
      JurisdictionSection/Jurisdiction, or if Summary/Jurisdiction/Subgroup is
      completed, then also match where Summary/Jurisdiction/Subgroup/TIN =
      JurisdictionSection/GLOBETax/ETR/SubGroup/TIN)
    description: >-
      The Routine Profits Test for the Transitional CbCR safe harbour must be
      completed, unless the Profit for the jurisdiction is 0 or a negative value.

  - number: 70054
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:Election/*
    when:
      isTrue: globe:Status
    absent: globe:RevocationYear
    implementation_notes: >-
      Encoded as the contrapositive: target each Election child (not its own
      RevocationYear, whose absence can't be asserted from itself), and when its
      Status is TRUE the RevocationYear must be absent. The Status guard uses isTrue
      so both boolean lexical forms ('true'/'1') are handled; Election children that
      carry no Status (e.g. the plain-boolean elections) fail the guard and are
      vacuously satisfied.
    rule: >-
      RevocationYear is only to be provided when the status is FALSE
    description: >-
      RevocationYear is only to be provided when the status is FALSE. When the Status
      is TRUE, the election has been made and has not been revoked. When the Status is
      FALSE, this indicates that the election has been revoked, as such the
      RevocationYear is only to be provided when the status is FALSE.

  - number: 70055
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:Election/globe:Art3.2.1.c/globe:OutstandingBalance
    almostEquals:
      expected: ../globe:QualOwnerIntentBalance + ../globe:Additions - ../globe:Reductions
    rule: >-
      The OutstandingBalance should be equal to the following calculation:
      QualOwnerIntentBalance + Additions - Reductions
    description: >-
      The OutstandingBalance amount does not equal the provided figures.

  - number: 70056
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:CEComputation/globe:Elections/*
    when:
      isTrue: globe:Status
    absent: globe:RevocationYear
    implementation_notes: >-
      Encoded as the contrapositive: target each Elections child (not its own
      RevocationYear, whose absence can't be asserted from itself), and when its
      Status is TRUE the RevocationYear must be absent. The Status guard uses isTrue
      so both boolean lexical forms ('true'/'1') are handled; Elections children that
      carry no Status (e.g. AggregatedReporting, the plain-boolean elections) fail the
      guard and are vacuously satisfied.
    rule: >-
      RevocationYear is only to be provided when the status is FALSE
    description: >-
      Revocation Year is only to be provided when the status is FALSE. When the Status
      is TRUE, the election has been made and has not been revoked. When the Status is
      FALSE, this indicates that the election has been revoked, as such the Revocation
      Year is only to be provided when the status is FALSE.

  - number: 70057
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:CEComputation/globe:TIN
    references:
      - GLOBEBody/JurisdictionSection/GLoBETax/ETR/ETRStatus/ETRComputation/CEComputation/Elections/AggregatedReporting
      - GLOBEBody/JurisdictionSection/GLoBETax/ETR/ETRStatus/ETRComputation/CEComputation/Elections/AggregatedReporting/TaxConsolGroupTIN
    element: TIN
    rule: >-
      If the Aggregated Reporting element is completed, then the CEComputations TIN
      should match the TaxConsol Group TIN.
    description: >-
      The TIN provided for the CE Computations must match to a TIN of the tax
      consolidated group used for purposes of Covered Taxes in the jurisdiction, if
      available.

  - number: 70058
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:CEComputation/globe:Elections/globe:Art7.6/globe:InvestmentEntityTIN
    references:
      - GLOBEBody/JurisdictionSection/GLoBETax/ETR/ETRStatus/ETRComputation/CEComputation/TIN
    element: InvestmentEntityTIN
    rule: >-
      The InvestmentEntityTIN must not match the CEComputations TIN
    description: >-
      The CE who is making the election under article 7.6 must be different from the
      investment entity who the election is made in respect of (i.e. label 3.2.4.5.1
      of the GIR must be different from 3.2.4.5.2).

  - number: 70059
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR
    distinct: globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:NetGlobeIncome/globe:Adjustments/globe:AdjustmentItem
    implementation_notes: >-
      Targets the ETR so the uniqueness check is scoped per ETR element; the
      operand selects the AdjustmentItem at this location.
    rule: >-
      Each code for this AdjustmentItem element cannot be used more than once per ETR
      element in the Jurisdiction Section.
    description: >-
      Each code for this AdjustmentItem element cannot be used more than once per ETR
      element in the Jurisdiction Section.

  - number: 70060
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:NetGlobeIncome
    when: globe:Adjustments/globe:AdjustmentItem = 'GIR2025'
    present: globe:IntShippingIncome
    implementation_notes: >-
      Targets the common NetGlobeIncome ancestor (not the IntShippingIncome the spec
      cites — you can't assert the presence of your own target) so the guard can read
      the sibling AdjustmentItem code. AdjustmentItem is repeatable, so the node-set
      `=` guard fires when any Adjustments carries GIR2025.
    rule: >-
      When the 'Adjustmentitem' element contains the value GIR2025 then the
      'IntShippingIncome' element must be completed
    description: >-
      An adjustment under article 3.3 has been made without completing the relevant
      table at 3.2.4.4.a.

  - number: 70061
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:Adjustments
    when:
      isTrue: ancestor::globe:ETR/globe:Election/globe:Art4.6.1
    allOf:
      - equals:
          actual: globe:AdjustmentItem
          expected: "'GIR2711'"
      - lessThan:
          actual: globe:Amount
          expected: 0
    rule: >-
      When the 'Art4.6.1' election has the value of TRUE, the AdjustmentItem for the
      AdjustedCovered Tax must contain GIR2711 and the 'amount' must be a negative
    description: >-
      When the 'Art4.6.1' election has the value of TRUE, the AdjustmentItem for the
      AdjustedCovered Tax must contain GIR2711 and the 'amount' must be a negative

  - number: 70062
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:Total
    when: ../globe:Adjustments/globe:AdjustmentItem = 'GIR2720'
    atLeast:
      expected: 0
    rule: >-
      If the Adjustmentitem element has the value of GIR2720, then the total of the
      AdjustedCovered Tax for the jurisdiction cannot be a negative.
    description: >-
      The Excess Negative Tax Expense cannot reduce the AdjustedCoveredTaxes for a
      jurisdiction below 0

  - number: 70063
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR
    distinct: globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:Adjustments/globe:AdjustmentItem
    implementation_notes: >-
      Targets the ETR so the uniqueness check is scoped per ETR element; the
      operand selects the AdjustmentItem at this location.
    rule: >-
      Each code for this AdjustmentItem element cannot be used more than once per ETR
      element in the Jurisdiction Section.
    description: >-
      Each code for this AdjustmentItem element cannot be used more than once per ETR
      element in the Jurisdiction Section.

  - number: 70064
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:PostFilingAdjust/globe:DeferTaxAsset/globe:Total
    almostEquals:
      expected: sum(../globe:AmountAttributed/globe:Amount)
    rule: >-
      The PostFilingAdjust/ DeferTaxAsset / Total should equal to the following
      calculation: sum of all 'PostFilingAdjust / DeferTaxAsset / AmountAttributed /
      Amount'
    description: >-
      The PostFilingAdjust / DeferTaxAsset / Total should equal to the following
      calculation: sum of all 'PostFilingAdjust / DeferTaxAsset / AmountAttributed /
      Amount

  - number: 70065
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:PostFilingAdjust/globe:CoveredTaxRefund/globe:Total
    almostEquals:
      expected: sum(../globe:AmountAttributed/globe:Amount)
    rule: >-
      The PostFilingAdjust / Covered TaxRefund / Total should equal to the following
      calculation: Sum of all 'PostFilingAdjust/ Covered TaxRefund / AmountAttributed
      / Amount
    description: >-
      The PostFilingAdjust / Covered TaxRefund / Total should equal to the following
      calculation: Sum of all 'PostFilingAdjust/ Covered TaxRefund / AmountAttributed
      / Amount

  - number: 70066
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:PostFilingAdjust/globe:DeferTaxAsset/globe:AmountAttributed/globe:Year
    atMost:
      expected: ancestor::globe:GLOBEBody/globe:FilingInfo/globe:Period/globe:Start
      type: year
    rule: >-
      The Year should correspond to or be before the YYYY value reported in the Period
      Start Date
    description: >-
      The provided year for the Loss Carry Back must be a prior Fiscal Year and must
      be the same as or prior to the Reporting Fiscal Year.
    implementation_notes: >-
      Compared by year only (both operands are xsd:date). Period Start is reached via
      ancestor::globe:GLOBEBody (per-filing). The spec's second cited path
      (Period/Start) is a reference, not a target, so it was dropped from targets.

  - number: 70067
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:PostFilingAdjust/globe:DeferTaxAsset
    distinct: globe:AmountAttributed/globe:Year
    implementation_notes: >-
      Targets DeferTaxAsset so uniqueness is scoped to its AmountAttributed
      entries. Year is an xsd:date reported at year granularity (YYYY-01-01) in
      the corpus, so distinctness of the date string is distinctness of the year.
    rule: >-
      If more than one AmountAttributed is provided, the years cannot be the same
    description: >-
      If more than one AmountAttributed is provided, the years cannot be the same

  - number: 70068
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:PostFilingAdjust/globe:CoveredTaxRefund/globe:AmountAttributed/globe:Year
    atMost:
      expected: ancestor::globe:GLOBEBody/globe:FilingInfo/globe:Period/globe:Start
      type: year
    rule: >-
      The Year should correspond to or be before the YYYY value reported in the Period
      Start Date
    description: >-
      The provided year for the Loss Carry Back must be a prior Fiscal Year and must
      be the same as or prior to the Reporting Fiscal Year.

  - number: 70069
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:PostFilingAdjust/globe:CoveredTaxRefund
    distinct: globe:AmountAttributed/globe:Year
    implementation_notes: >-
      Targets CoveredTaxRefund so uniqueness is scoped to its AmountAttributed
      entries. Year is an xsd:date reported at year granularity (YYYY-01-01) in
      the corpus, so distinctness of the date string is distinctness of the year.
    rule: >-
      If more than one AmountAttributed is provided, the years cannot be the same
    description: >-
      If more than one AmountAttributed is provided, the years cannot be the same

  - number: 70070
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:DeemedDistTax/globe:Election/globe:Recapture/globe:Year
    atMost:
      expected: ancestor::globe:GLOBEBody/globe:FilingInfo/globe:Period/globe:End
      type: year
    rule: >-
      The year cannot be a date which is after the Period End Date
    description: >-
      The deemed distribution tax election under article 7.3.1 has been elected for a
      year which is not the Reporting Fiscal Year or a prior Fiscal Year
    implementation_notes: >-
      Assumes a year-only comparison, reading the Recapture Year as a fiscal year
      against the Period End year, scoped via ancestor::globe:GLOBEBody.

  - number: 70071
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:DeemedDistTax/globe:Election/globe:Recapture/globe:Year
    atLeast:
      expected: ancestor::globe:GLOBEBody/globe:FilingInfo/globe:Period/globe:End
      type: year
      offset: -3
    rule: >-
      The year cannot contain a date which is 4 years or more before the Period End
      Date (i.e. Reporting Fiscal Year and previous 3 Fiscal Years)
    description: >-
      The year cannot contain a date which is 4 years or more before the Period End
      Date (i.e. Reporting Fiscal Year and previous 3 Fiscal Years)
    implementation_notes: >-
      Lower bound only; 70070 covers the upper bound. "4 years or more before"
      maps to year >= Period End year - 3 (offset -3).

  - number: 70072
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:DeemedDistTax/globe:Election/globe:Recapture/globe:EndAmount
    almostEquals:
      expected: ../globe:StartAmount - ../globe:TotalDDT
    rule: >-
      The EndAmount is equal to the following calculation: StartAmount - TotalDDT
    description: >-
      The EndAmount is equal to the following calculation: StartAmount - TotalDDT

  - number: 70073
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:DeemedDistTax/globe:Election/globe:Recapture/globe:EndAmount
    atLeast:
      expected: 0
    rule: >-
      The EndAmount must not be a negative.
    description: >-
      The EndAmount must not be a negative.

  - number: 70074
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:DeemedDistTax/globe:Election/globe:Recapture/globe:TotalDDT
    almostEquals:
      expected: >-
        sum(../globe:DDTYear-0) +
        sum(../globe:DDTYear-1) +
        sum(../globe:DDTYear-2) +
        sum(../globe:DDTYear-3)
    rule: >-
      The TotalDDT is equal to the following calculation: DDTYear-0 + DDTYear-1 +
      DDTYear-2 + DDTYear-3
    description: >-
      The TotalDDT is equal to the following calculation: DDTYear-0 + DDTYear-1 +
      DDTYear-2 + DDTYear-3

  - number: 70075
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:DeemedDistTax/globe:Election/globe:Recapture/globe:DDTYear-0
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:DeemedDistTax/globe:Election/globe:Recapture/globe:DDTYear-1
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:DeemedDistTax/globe:Election/globe:Recapture/globe:DDTYear-2
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:DeemedDistTax/globe:Election/globe:Recapture/globe:DDTYear-3
    when:
      equals:
        actual: ../globe:Year
        expected: ancestor::globe:GLOBEBody/globe:FilingInfo/globe:Period/globe:End
        type: year
    equals:
      expected: 0
    rule: >-
      When the Year equals YYYY value reported in the Period End Date, then the
      following elements should be completed by entering "0": DDTYear-0, DDTYear-1,
      DDTYear-2 and DDTYear-3
    description: >-
      When the Year equals YYYY value reported in the Period End Date, then the
      following elements should be completed by entering "0": DDTYear-0, DDTYear-1,
      DDTYear-2 and DDTYear-3
    implementation_notes: >-
      The four DDTYear-N are separate targets so each is checked independently; the
      `when` guard compares the sibling Recapture/Year against Period End by year.

  - number: 70076
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:TransBlendCFC/globe:Total
    almostEquals:
      expected: sum(../globe:CFCJur/globe:Allocation/globe:AggAllocTax)
    rule: >-
      The TransBlendCFC total should equal the following calculation: Sum of all
      'AggAllocTax'
    description: >-
      The TransBlend CFC total should equal the following calculation: Sum of all
      'AggAllocTax'

  - number: 70077
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:DeferTaxAdjustAmt/globe:Total
    almostEquals:
      expected: >-
        ../globe:PreRecast +
        sum(../globe:Recast/globe:Lower) - sum(../globe:Recast/globe:Higher)
    rule: >-
      The total element should be equal to the following calculation: PreRecast +
      Recast/Lower - Recast/Higher If Recast/Lower and/or Recast/Higher is not
      provided, then assumed to be 0.
    description: >-
      The total element should be equal to the following calculation: PreRecast +
      Recast/Lower - Recast/Higher If Recast/Lower and/or Recast/Higher is not
      provided, then assumed to be 0.

  - number: 70078
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:DeferTaxAdjustAmt/globe:BefRecastAdjust
    almostEquals:
      expected: >-
        ../globe:DefTaxAmt -
        ../globe:DiffCarryValue +
        ../globe:GLoBEValue
    rule: >-
      The BefRecastAdjust element should be equal to the following calculation:
      DefTaxAmt - DiffCarryValue + GLoBEValue.
    description: >-
      The BefRecastAdjust element should be equal to the following calculation:
      DefTaxAmt - DiffCarryValue + GLoBEValue

  - number: 70079
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:DeferTaxAdjustAmt/globe:PreRecast
    almostEquals:
      expected: ../globe:BefRecastAdjust + ../globe:TotalAdjust
    rule: >-
      The PreRecast element should be equal to the following calculation:
      BefRecastAdjust + TotalAdjust
    description: >-
      The PreRecast element should be equal to the following calculation:
      BefRecastAdjust + TotalAdjust

  - number: 70080
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR
    distinct: globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:DeferTaxAdjustAmt/globe:Adjustments/globe:AdjustmentItem
    implementation_notes: >-
      Targets the ETR so the uniqueness check is scoped per ETR element; the
      operand selects the AdjustmentItem at this location.
    rule: >-
      Each code for this AdjustmentItem element cannot be used more than once per ETR
      element in the Jurisdiction Section.
    description: >-
      Each code for this AdjustmentItem element cannot be used more than once per ETR
      element in the Jurisdiction Section.

  - number: 70081
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:DeferTaxAdjustAmt/globe:Transition/globe:DeferredTaxAssets/globe:Total
    almostEquals:
      expected: >-
        ../globe:DeferredTaxAssetStart + ../globe:DeferredTaxAssetRecast - ../globe:DeferredTaxAssetExcluded
    rule: >-
      The total element should be equal to the following calculation:
      DeferredTaxAssetStart - DeferredTaxAssetExcluded OR
      DeferredTaxAssetRecast - DeferredTaxAssetExcluded
    description: >-
      The total element should be equal to the following calculation:
      DeferredTaxAssetStart - DeferredTaxAssetExcluded OR
      DeferredTaxAssetRecast - DeferredTaxAssetExcluded

  - number: 70082
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:DeferTaxAdjustAmt/globe:Transition/globe:DeferredTaxAssets
    anyOf:
      - equals:
          actual: globe:DeferredTaxAssetStart
          expected: 0
      - equals:
          actual: globe:DeferredTaxAssetRecast
          expected: 0
    rule: >-
      When the DeferredTaxAssets element is provided, then one of
      DeferredTaxAssetStart or DeferredTaxAssetRecast must contain the value of '0'
    description: >-
      To calculate the Deferred tax assets taken into account for GloBE purposes,
      ensure that either the DeferredTaxAsset at the start of the year or the
      DeferredTaxAsset recast at the minimum rate is used, both values cannot be
      used. Therefore, ensure that when calculating the DeferredTaxAsset/total
      element, that one of DeferredTaxAssetStart or DeferredTaxAssetRecast contains
      the value of 0

  - number: 70083
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:ExcessNegTaxExpense/globe:Remaining
    almostEquals:
      expected: >-
        ../globe:PriorYearBalance +
        ../globe:GeneratedInRFY - ../globe:UtilizedInRFY
    rule: >-
      The Remaining value should equal the following calculation: Prior YearBalance +
      GeneratedInRFY - UtilizedInRFY
    description: >-
      The Remaining value should equal the following calculation: Prior YearBalance +
      GeneratedInRFY - UtilizedInRFY

  - number: 70084
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:Adjustments/globe:Amount
    references:
      - GLOBEBody/JurisdictionSection/GLoBETax/ETR/ETRStatus/ETRComputation/OverallComputation/ExcessNegTaxExpense/GeneratedInRFY
    element: GeneratedInRFY
    rule: >-
      If the Adjustmentitem element has the value of GIR2719, then the value reported
      in the 'amount' element should be the same as the integer reported in the
      GeneratedInRFY element
    description: >-
      In the 'Adjusted Covered Tax' element, a lodger can select from a list of
      possible adjustments, and must report the following 'Name of adjustment
      (enumeration) and the amount of said adjustment. If the selected 'adjustment'
      has the 'AdjustmentItem' value of 'GIR2719', then the value which is entered
      into the 'amount' element should be an exact match to the integer reported in
      the GeneratedInRFY element.

  - number: 70085
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdjustedCoveredTax/globe:Adjustments/globe:AdjustmentItem
    references:
      - GLOBEBody/JurisdictionSection/GLoBETax/ETR/ETRStatus/ETRComputation/OverallComputation/ExcessNegTaxExpense/UtilizedInRFY
    element: UtilizedInRFY
    rule: >-
      If the Adjustmentitem element has the value of GIR2720, then the value reported
      in the 'amount' element should be the same as the integer reported in the
      UtilizedInRFY element
    description: >-
      If the AdjustedItem element has the value of GIR2720, then the value reported in
      the 'amount' element should be the same as the integer reported in the
      UtilizedInRFY element

  - number: 70086
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:ExcessProfits
    anyOf:
      - almostEquals:
          expected: ../globe:NetGlobeIncome/globe:Total - sum(../globe:SubstanceExclusion/globe:Total)
      - allOf:
          - atMost:
              actual: ../globe:NetGlobeIncome/globe:Total - sum(../globe:SubstanceExclusion/globe:Total)
              expected: 0
          - equals:
              expected: 0
    implementation_notes: >-
      ExcessProfits is the difference floored at 0: it equals NetGlobeIncome/Total
      - SubstanceExclusion/Total, or 0 when that difference is negative
      (SubstanceExclusion/Total summed so a missing value counts as 0).
    rule: >-
      The ExcessProfits integer is equal to the following calculation:
      NetGlobeIncome/Total - SubstanceExclusion/Total. If SubstanceExclusion/Total
      is not provided, then should be treated as a 0 value. If the
      NetGlobeIncome/Total - SubstanceExclusion/Total results in a number less than
      0, then ExcessProfits must equal to 0.
    description: >-
      The ExcessProfits integer is equal to the following calculation:
      NetGlobeIncome/Total - SubstanceExclusion/Total. If SubstanceExclusion/Total
      is not provided, then should be treated as a 0 value. If the
      NetGlobeIncome/Total - SubstanceExclusion/Total results in a number less than
      0, then ExcessProfits must equal to 0.

  - number: 70087
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:SubstanceExclusion/globe:Total
    almostEquals:
      expected: >-
        ../globe:PayrollCost * ../globe:PayrollMarkUp +
        ../globe:TangibleAssetValue * ../globe:TangibleAssetMarkup
    rule: >-
      The Total integer for the SubstanceExclusion is equal to the following
      calculation: PayrollCost * PayrollMarkUp + TangibleAssetValue * TangibleAssetMarkup
    description: >-
      The Total integer for the SubstanceExclusion is equal to the following
      calculation: PayrollCost * PayrollMarkUp + TangibleAssetValue *
      TangibleAssetMarkup

  - number: 70088
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdditionalTopUpTax/globe:Art4.1.5
    references:
      - GLOBEBody/JurisdictionSection/GLoBETax/ETR/ETRStatus/ETRComputation/OverallComputation/NetGloboIncome/Total
    element: Art4.1.5
    rule: >-
      The Art4.1.5 element must be completed if the
      OverallComputation/NetGlobeIncome/Total is a negative value
    description: >-
      The Art4.1.5 element must be completed if the
      OverallComputation/NetGlobeIncome/Total is a negative value

  - number: 70089
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdditionalTopUpTax/globe:Art4.1.5/globe:AdjustedCoveredTax
    lessThan:
      expected: 0
    rule: >-
      The AdjustedCoveredTax value should be a negative value
    description: >-
      The AdjustedCoveredTax value should be a negative value

  - number: 70090
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdditionalTopUpTax/globe:Art4.1.5/globe:GlobeLoss
    equals:
      expected: ancestor::globe:OverallComputation/globe:NetGlobeIncome/globe:Total
    rule: >-
      The GlobeLoss element must be the same as
      OverallComputation/NetGlobeIncome/Total value.
    description: >-
      The GlobeLoss element must be the same as
      OverallComputation/NetGlobeIncome/Total value.

  - number: 70091
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdditionalTopUpTax/globe:Art4.1.5/globe:ExpectedAdjustedCoveredTax
    almostEquals:
      expected: ../globe:GlobeLoss * 0.15
    rule: >-
      The ExpectedAdjustedCoveredTax must be equal to the following calculation:
      GlobeLoss * 15%
    description: >-
      The ExpectedAdjustedCoveredTax must be equal to the following calculation:
      GlobeLoss * 15%

  - number: 70092
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdditionalTopUpTax/globe:Art4.1.5/globe:AdditionalTopUpTax
    anyOf:
      - almostEquals:
          expected: ../globe:ExpectedAdjustedCoveredTax - ../globe:AdjustedCoveredTax
      - allOf:
          - atMost:
              actual: ../globe:ExpectedAdjustedCoveredTax - ../globe:AdjustedCoveredTax
              expected: 0
          - equals:
              expected: 0
    implementation_notes: >-
      AdditionalTopUpTax is the difference floored at 0: it equals
      ExpectedAdjustedCoveredTax - AdjustedCoveredTax, or 0 when that difference is
      negative.
    rule: >-
      The AdditionalTopUpTax integer is equal to the following calculation:
      ExpectedAdjustedCoveredTax - AdjustedCoveredTax. If the
      ExpectedAdjustedCoveredTax - AdjustedCoveredTax results in a number less than
      0, then AdditionalTopUpTax must equal to 0.
    description: >-
      The AdditionalTopUpTax integer is equal to the following calculation:
      ExpectedAdjustedCoveredTax - AdjustedCoveredTax. If the
      ExpectedAdjustedCoveredTax - AdjustedCoveredTax results in a number less than
      0, then AdditionalTopUpTax must equal to 0.

  - number: 70093
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdditionalTopUpTax/globe:NONArt4.1.5/globe:Year
    atMost:
      expected: ancestor::globe:GLOBEBody/globe:FilingInfo/globe:Period/globe:End
      type: year
    rule: >-
      The Year must not be greater than the YYYY value reported in the Period End Date
    description: >-
      The Year must not be greater than the YYYY value reported in the Period End Date

  - number: 70094
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdditionalTopUpTax/globe:NONArt4.1.5/globe:Year
    when: ../globe:Articles = 'GIR2605'
    atMost:
      expected: ancestor::globe:GLOBEBody/globe:FilingInfo/globe:Period/globe:End
      type: year
      offset: -4
    rule: >-
      When the Articles contain the value of GIR2605, then the Year should be at least
      '4 years' before the YYYY value reported in the Period End Date.
    description: >-
      When the Articles contain the value of GIR2605, then the Year should be at least
      '4 years' before the YYYY value reported in the Period End Date
    implementation_notes: >-
      Gated on the sibling Articles (repeatable, so the guard matches if any equals
      GIR2605). "At least 4 years before" maps to year <= Period End year - 4.

  - number: 70095
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdditionalTopUpTax/globe:NONArt4.1.5/globe:Year
    when: ../globe:Articles = 'GIR2602'
    equals:
      expected: ancestor::globe:GLOBEBody/globe:FilingInfo/globe:Period/globe:End
      type: year
      offset: -5
    rule: >-
      When the Articles contain the value of GIR2602, then the Year should be the
      fifth Fiscal Year preceding the YYYY value reported in the Period End Date.
    description: >-
      When the Articles contain the value of GIR2602, then the Year should be the
      fifth Fiscal Year preceding the YYYY value reported in the Period End Date

  - number: 70096
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:OverallComputation/globe:AdditionalTopUpTax/globe:NONArt4.1.5/globe:AdditionalTopUpTax
    almostEquals:
      expected: ../globe:Recalculated/globe:TopUpTax - ../globe:Previous/globe:TopUpTax
    rule: >-
      Additional TopUpTax is calculated as follows: Recalculated/TopUpTax -
      Previous/TopUpTax
    description: >-
      AdditionalTopUpTax is calculated as follows: Recalculated/TopUpTax -
      Previous/TopUpTax

  - number: 70097
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:LowTaxJurisdiction/globe:LTCE/globe:IIR/globe:ParentEntity/globe:InclusionRatio
    when: ancestor::globe:IIR/globe:NetGlobeIncome != 0
    almostEquals:
      expected: >-
        (ancestor::globe:IIR/globe:NetGlobeIncome - sum(../globe:OtherOwnershipAllocation))
        div ancestor::globe:IIR/globe:NetGlobeIncome
    implementation_notes: >-
      Guarded on a non-zero NetGlobeIncome since the ratio is otherwise undefined
      (divide-by-zero); OtherOwnershipAllocation is summed so a missing value is 0.
    rule: >-
      The InclusionRatio must equal the following calculation: (NetGlobeIncome - Other
      OwnershipAllocation) DIVIDED by NetGlobeIncome
    description: >-
      The InclusionRatio must equal the following calculation: (NetGlobelncome - Other
      OwnershipAllocation) DIVIDED by NetGlobelncome

  - number: 70098
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:LowTaxJurisdiction/globe:LTCE/globe:IIR/globe:ParentEntity/globe:TopUpTaxShare
    almostEquals:
      expected: ancestor::globe:IIR/globe:TopUpTax * ../globe:InclusionRatio
    rule: >-
      The IIR/ParentEntity/TopUpTaxShare must equal the following calculation:
      IIR/TopUpTax * InclusionRatio
    description: >-
      The IIR/ParentEntity/TopUpTaxShare must equal the following calculation:
      TopUpTax * InclusionRatio

  - number: 70099
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody
    almostEquals:
      actual: sum(globe:UTPRAttribution/globe:Attribution/globe:UTPRTopUpTaxAttributed)
      expected: sum(globe:JurisdictionSection/globe:LowTaxJurisdiction/globe:UTPR/globe:UTPRCalculation/globe:TotalUTPRTopUpTax)
    implementation_notes: >-
      A whole-message aggregate (sum across all jurisdictions), so the target is
      the GLOBEBody to give the two sums a single context.
    rule: >-
      The sum of 'UTPRTopUp TaxAttributed' should be equal to the sum of
      TotalUTPRTopUpTax amount in the JurisdictionSection element in respect to all
      jurisdictions.
    description: >-
      The sum of 'UTPRTopUp TaxAttributed" should be equal to the sum of
      TotalUTPRTopUpTax amount in the JurisdictionSection element in respect to all
      jurisdictions.

  - number: 70100
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:LowTaxJurisdiction/globe:UTPR/globe:UTPRCalculation
    when: globe:TotalUTPRTopUpTax > 0
    present: ancestor::globe:GLOBEBody/globe:UTPRAttribution
    implementation_notes: >-
      Targets the UTPRCalculation (so the gating value is a child of the context) and
      reaches the message-level UTPRAttribution via the GLOBEBody ancestor. Targeting
      UTPRCalculation covers "is provided"; the TotalUTPRTopUpTax > 0 guard covers the
      value condition.
    rule: >-
      When the UTPRCalculation is provided and the TotalUTPRTopUpTax element contains
      a value greater than 0 then the UTPRAttribution must be completed.
    description: >-
      A UTPR Top up tax has been reported and the corresponding attribution of the
      UTRP has not been reported. Please complete the UTPRAttribution element.

  - number: 70101
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:UTPRAttribution/globe:Attribution
    when:
      notEquals:
        actual: globe:UTPRTopUpTaxCarryForward
        expected: 0
    present: globe:Employees
    rule: >-
      The Employees must be completed unless the UTPRTopUpTaxCarryForward contains the
      value of '0'
    description: >-
      The employees element must be completed as there is no carry forward amount that
      still needs to be allocated to this jurisdiction

  - number: 70102
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:UTPRAttribution/globe:Attribution
    when:
      notEquals:
        actual: globe:UTPRTopUpTaxCarryForward
        expected: 0
    present: globe:TangibleAssetValue
    rule: >-
      The TangibleAssetValue must be completed unless the UTPRTopUpTaxCarryForward
      contains the value of '0'
    description: >-
      The TangibleAssetValue element must be completed as there is no carry forward
      amount that still needs to be allocated to this jurisdiction

  - number: 70103
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:UTPRAttribution/globe:Attribution/globe:UTPRPercentage
    references:
      - GLOBEBody/UTPRAttribution/Attribution/UTPRTopUpTaxCarryForward
    element: UTPRPercentage
    rule: >-
      The UTPRPercentage must be 0% when the UTPRTopUp TaxCarryForward is greater than
      0. If the UTPRTopUpTaxCarryForward is 0, then the UTPRPercentage can only be 0%
      when all UTPR jurisdictions have a UTPR Percentage of 0 in the Reporting Fiscal
      Year.
    description: >-
      The UTPRPercentage must be 0% when the UTPRTopUpTaxCarryForward is greater than
      0. If the UTPRTopUpTaxCarryForward is 0, then the UTPRPercentage can only be 0%
      when all UTPR jurisdictions have a UTPR Percentage of 0 in the Reporting Fiscal
      Year.

  - number: 70104
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:UTPRAttribution/globe:Attribution/globe:UTPRTopUpTaxCarriedForward
    atLeast:
      expected: 0
    rule: >-
      The UTPRTopUpTaxCarriedForward cannot be a negative value.
    description: >-
      The UTPRTopUpTaxCarriedForward must be a positive value

  - number: 70105
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:UTPRAttribution/globe:Attribution/globe:UTPRTopUpTaxCarriedForward
    almostEquals:
      expected: >-
        sum(../globe:UTPRTopUpTaxCarryForward) +
        sum(../globe:UTPRTopUpTaxAttributed) -
        sum(../globe:AddCashTaxExpense)
    rule: >-
      The UTPRTopUpTaxCarriedForward is equal to the following calculation
      UTPRTopUpTaxCarryForward + UTPRTopUpTaxAttributed - AddCashTaxExpense
    description: >-
      The UTPRTopUpTaxCarriedForward is equal to the following calculation
      UTPRTopUpTaxCarryForward + UTPRTopUpTaxAttributed - AddCashTaxExpense

  - number: 70106
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:CEComputation/globe:AdjustedFANIL/globe:Adjustment/globe:CrossBorderAdjustments/globe:OtherTIN
    notEquals:
      expected: ancestor::globe:CEComputation/globe:TIN
    rule: >-
      The Other TIN must not be the same as the CEComputations/TIN.
    description: >-
      The Other TIN must not be the same as the CEComputations/TIN.

  - number: 70107
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:CEComputation/globe:AdjustedFANIL/globe:Adjustment/globe:CrossBorderAdjustments
    absent: ../globe:UPEAdjustments/globe:Reductions/globe:Exception[. = 'true' or . = '1']
    target_does_not_exist_in_test_files:
      - ok-03.xml
      - ok-04.xml
    rule: >-
      Where the Exception element is TRUE, then the CrossBorderAdjustements must not
      be provided for that CEComputation section.
    description: >-
      When the UPE is wholly owned by holders of Ownership Interests in respect of
      which Article 7.1 applies, and the Filing Constituent Entity reported
      "exception, wholly-owned" in column 3.2.4.1.d.5, the Filing Constituent Entity
      shall not complete Table 3.2.4.1.c with respect to the UPE (and with respect to
      Tax Transparent Entities wholly owned by the UPE through a Tax Transparent
      Structure and located in the UPE jurisdiction to which Article 3.5.1(b)
      applies).
      (see note 3.2.4.1.c of the GIR)
    implementation_notes: >-
      The path in the specification wrongly contains the Total element.

  - number: 70108
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:CEComputation/globe:AdjustedFANIL/globe:Adjustment/globe:UPEAdjustments
    when: >-
      globe:Basis = 'GIR1901' or globe:Basis = 'GIR1902' or globe:Basis = 'GIR1905' or globe:Basis = 'GIR1906'
    anyOf:
      - present: globe:IdentificationOfOwners/globe:IndOwners/globe:TaxRate
      - present: globe:IdentificationOfOwners/globe:EntityOwner/globe:TaxRate
    rule: >-
      When the 'Basis' is GIR1901, GIR1902, GIR1905, GIR1906 then the
      'EntityOwner/TaxRate' element or 'IndOwners/TaxRate' element must be completed
    description: >-
      When the 'Basis' is GIR1901, GIR1902, GIR1905, GIR1906 then the
      'EntityOwner/TaxRate' element or 'IndOwners/TaxRate' element must be completed

  - number: 70109
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:CEComputation/globe:AdjustedFANIL/globe:Adjustment/globe:UPEAdjustments
    when: globe:Basis = 'GIR1907'
    present: globe:IdentificationOfOwners/globe:IndOwners/globe:ResCountryCode
    rule: >-
      When the 'Basis' is GIR1907 then the 'ResCountryCode' element must be completed
      (i.e. the choice must be 'IndOwners')
    description: >-
      When the 'Basis' is GIR1907 then the 'ResCountryCode' element must be completed
      (i.e. the choice must be 'IndOwners')

  - number: 70110
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:CEComputation/globe:AdjustedFANIL/globe:Adjustment/globe:UPEAdjustments
    when: globe:Basis = 'GIR1903' or globe:Basis = 'GIR1908'
    present: globe:IdentificationOfOwners/globe:IndOwners
    rule: >-
      When the 'Basis' is GIR1903 or GIR1908, then the IndOwners element must be
      completed
    description: >-
      When the 'Basis' is GIR1903 or GIR1908, then the IndOwners element must be
      completed

  - number: 70111
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:CEComputation/globe:AdjustedFANIL/globe:Adjustment/globe:UPEAdjustments
    when: globe:Basis = 'GIR1904' or globe:Basis = 'GIR1909'
    present: globe:IdentificationOfOwners/globe:EntityOwner/globe:ExTypeOfEntity
    rule: >-
      When the 'Basis' is GIR1904 or GIR1909, then the 'ExTypeOfEntity' must be
      completed (i.e. the choice must be 'EntityOwner')
    description: >-
      When the 'Basis' is GIR1904 or GIR1909, then the 'ExTypeOfEntity' must be
      completed (i.e. the choice must be 'EntityOwner')

  - number: 70112
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:CEComputation/globe:AdjustedFANIL/globe:Adjustment/globe:UPEAdjustments
    when: globe:Basis = 'GIR1904'
    notEquals:
      actual: globe:IdentificationOfOwners/globe:EntityOwner/globe:ExTypeOfEntity
      expected: "'GIR2805'"
    rule: >-
      When the 'Basis' is GIR1904, then the 'ExTypeOfEntity' must not contain the
      value of GIR2805.
    description: >-
      When the 'Basis' is GIR1904, then the 'ExTypeOfEntity' must not contain the
      value of GIR2805.
    implementation_notes: >-
      Only the value is checked here; ExTypeOfEntity's presence under GIR1904 is
      already enforced by 70111. notEquals on an absent ExTypeOfEntity is vacuously
      satisfied, so this rule purely guards against the GIR2805 value.

  - number: 70113
    targets: 
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:CEComputation/globe:AdjustedFANIL/globe:Adjustment/globe:UPEAdjustments
    when: globe:Basis = 'GIR1909'
    notEquals:
      actual: globe:IdentificationOfOwners/globe:EntityOwner/globe:ExTypeOfEntity
      expected: "'GIR2804'"
    rule: >-
      When the 'Basis' is GIR1909, then the 'ExTypeOfEntity' must not contain the
      value of GIR2804.
    description: >-
      When the 'Basis' is GIR1909, then the 'ExTypeOfEntity' must not contain the
      value of GIR2804.
    implementation_notes: >-
      Only the value is checked here; ExTypeOfEntity's presence under GIR1909 is
      already enforced by 70111. notEquals on an absent ExTypeOfEntity is vacuously
      satisfied, so this rule purely guards against the GIR2804 value.

  - number: 70114
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:CEComputation/globe:NetGlobeIncome/globe:Adjustments
    when: count(globe:Amount) = 2
    allOf:
      - present: globe:Amount[. < 0]
      - present: globe:Amount[. > 0]
    rule: >-
      If two values are provided for the'Amount', then one value must be negative and
      the other value positive (i.e. both values of the integer cannot be positive or
      negative)
    description: >-
      If two values are provided for the'Amount', then one value must be negative and
      the other value positive (i.e. both values of the integer cannot be positive or
      negative)
    implementation_notes: >-
      "One negative and one positive" is expressed as the presence of an Amount < 0
      and the presence of an Amount > 0; combined with the count = 2 guard this forces
      exactly one of each sign (and rejects a 0, which is neither).

  - number: 70115
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:CEComputation
    when: >-
      globe:NetGlobeIncome/globe:Adjustments/globe:AdjustmentItem = 'GIR2022' or
      globe:NetGlobeIncome/globe:Adjustments/globe:AdjustmentItem = 'GIR2023'
    present: globe:AdjustedFANIL/globe:Adjustment/globe:UPEAdjustments
    rule: >-
      When the 'Adjustmentitem' element contains the value GIR2022 and/or GIR2023 then
      the 'UPEAdjustments' element must be completed
    description: >-
      An adjustment under article 7.1 or 7.2 can only be selected when the table at
      3.2.4.1.d has been completed.
    implementation_notes: >-
      We use the common parent element as a target otherwise we cannot check for the non-existence of the UPEAdjustments element.

  - number: 70116
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:CEComputation/globe:NetGlobeIncome
    when: globe:Adjustments/globe:AdjustmentItem = 'GIR2025'
    present: globe:IntShippingIncome
    rule: >-
      When the 'AdjustmentItem' element contains the value GIR2025 then the
      'IntShippingIncome' element must be completed
    description: >-
      An adjustment under article 3.3 has been made without completing the relevant
      table at 3.2.4.4.a.
    implementation_notes: >-
      We use the common parent element as a target otherwise we cannot check for the non-existence of the IntShippingIncome element.

  - number: 70117
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:CEComputation
    when: globe:NetGlobeIncome/globe:Adjustments/globe:AdjustmentItem = 'GIR2024'
    present: globe:Elections/globe:Art7.6
    rule: >-
      When the 'Adjustmentitem' element contains the value GIR2024 then the 'Art7.6'
      element must be completed
    description: >-
      An adjustment under article 7.6 has been made without completing the relevant
      table at 3.2.4.5.
    implementation_notes: >-
      We use the common parent element as a target otherwise we cannot check for the non-existence of the Art7.6 element.

  - number: 70118
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:CEComputation/globe:AdjustedCoveredTax/globe:Adjustments/globe:Amount
    element: Amount
    rule: >-
      If two values are provided for the'Amount', then one value must be negative and
      the other value positive (i.e. both values of the integer cannot be positive or
      negative)
    description: >-
      If two values are provided for the'Amount', then one value must be negative and
      the other value positive (i.e. both values of the integer cannot be positive or
      negative)

  - number: 70119
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR
    distinct: globe:ETRStatus/globe:ETRComputation/globe:CEComputation/globe:AdjustedCoveredTax/globe:Adjustments/globe:AdjustmentItem
    implementation_notes: >-
      Targets the ETR so the uniqueness check is scoped per ETR element (across
      all of its CEComputations, per the literal "per ETR" wording); the operand
      selects the AdjustmentItem at this location.
    rule: >-
      Each code for this AdjustmentItem element cannot be used more than once per ETR
      element in the Jurisdiction Section.
    description: >-
      Each code for this AdjustmentItem element cannot be used more than once per ETR
      element in the Jurisdiction Section.

  - number: 70120
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:CEComputation/globe:AdjustedCoveredTax/globe:DeferTaxAdjustAmt/globe:Total
    almostEquals:
      expected: >-
        sum(../globe:DeferTaxExpense) +
        sum(../globe:Adjustment/globe:Amount) +
        sum(../globe:Adjustment/globe:Recast/globe:Higher) +
        sum(../globe:Adjustment/globe:Recast/globe:Lower)
    rule: >-
      The Total element shall equal the following calculation: Defer TaxExpense + Sum
      of all values reported in Adjustments/Amount + Recast/Higher + Recast/Lower (if
      any element is not provided, it is treated as a 0 value)
    description: >-
      The Total element shall equal the following calculation: DeferTaxExpense + Sum
      of all values reported in Adjustments/Amount + Recast/Higher + Recast/Lower (if
      any element is not provided, it is treated as a 0 value)
    implementation_notes: >-
      According to the schema the Recast element is nested in Adjustment.

  - number: 70121
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR
    distinct: globe:ETRStatus/globe:ETRComputation/globe:CEComputation/globe:AdjustedCoveredTax/globe:DeferTaxAdjustAmt/globe:Adjustment/globe:AdjustmentItem
    implementation_notes: >-
      Targets the ETR so the uniqueness check is scoped per ETR element (across
      all of its CEComputations, per the literal "per ETR" wording); the operand
      selects the AdjustmentItem at this location.
    rule: >-
      Each code for this Adjustmentltem element cannot be used more than once per ETR
      element in the Jurisdiction Section.
    description: >-
      Each code for this Adjustmentltem element cannot be used more than once per ETR
      element in the Jurisdiction Section.

  - number: 70122
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:CEComputation/globe:AdjustedCoveredTax/globe:DeferTaxAdjustAmt/globe:Adjustment
    when: count(globe:Amount) = 2
    allOf:
      - present: globe:Amount[. < 0]
      - present: globe:Amount[. > 0]
    rule: >-
      If two values are provided for the Amount element, then one value must be
      negative and the other value must be positive (i.e. the values of the integer
      cannot be both positive or negative)
    description: >-
      If two values are provided for the Amount element, then one value must be
      negative and the other value must be positive (i.e. the values of the integer
      cannot be both positive or negative)
    implementation_notes: >-
      Targets the Adjustment (parent of Amount) so the count = 2 guard and the two
      sign-presence checks are evaluated once per Adjustment rather than once per
      Amount; see 70114 for the same "one negative and one positive" idiom.

  - number: 70123
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:CEComputation/globe:AdjustedIncomeTax/globe:CrossAllocation/globe:Additions
    atLeast:
      expected: 0
    rule: >-
      The Additions element must not have a negative value.
    description: >-
      The Additions element must not have a negative value.

  - number: 70124
    targets:
      - /globe:GLOBE_OECD/globe:GLOBEBody/globe:JurisdictionSection/globe:GLoBETax/globe:ETR/globe:ETRStatus/globe:ETRComputation/globe:CEComputation/globe:AdjustedIncomeTax/globe:CrossAllocation/globe:Reductions
    atMost:
      expected: 0
    rule: >-
      The Reductions element must not be a positive value.
    description: >-
      The Reductions element must not be a positive value.
