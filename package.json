{
  "name": "oecd-gir-viewer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "build:watch": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.23.1",
    "esbuild-plugin-copy": "^2.1.1"
  }
}
